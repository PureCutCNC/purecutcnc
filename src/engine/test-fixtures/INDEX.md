# INDEX — src/engine/test-fixtures/

Committed fixtures used by engine tests.

## Files
- `3d-imported-block-test3.camj` — imported-surface waterline regression project used by `toolpaths/finishSurface.test.ts`
