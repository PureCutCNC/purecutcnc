# INDEX — src/engine/toolpaths/

Toolpath generators. Each file owns one strategy. `index.ts` re-exports everything.

## Operations (per-strategy files)
- `pocket.ts` — pocket clearing (offset-based clearing of an enclosed area). Seeded pockets (issue #575) schedule offset work as a frontier of ring units with child-completion dependencies instead of draining a whole tree after its first selection: after every seed stack or offset unit the next entry is re-chosen globally from the actual endpoint, so seed stacks and offset branches interleave; links inside one tree stay direct (S-link eligible) and cross-section travel stays at safe Z. Unseeded pockets keep the legacy whole-tree sections, which is also the byte-identical fallback `seeded_offset` keeps when no seed fits. Also includes the `roundLinkCorners` tangential S-links (issue #545): the straight ring-to-ring link is replaced by an S-curve that departs ring N along its travel tangent and arrives on a vertex of ring N+1 along its travel tangent, with the arrival point free to slide along the next ring; when no S fits the cleared domain and budget, the straight link stays. The finish floor cuts the same ring set as the rough floor: per tree node both the outer contour and the island contours (`loops 'all'`), with island holes grown by `jtRound` when `roundOutsideCorners` is on (issue #550) — before this the finish skipped the island contours, leaving the corner lens between the rounded wall contour and the floor rings uncut (0.015495 in² of reachable stock on the repro; clear after). The finish floor also honours `pocketPattern: 'seeded_offset'` (issue #579): seeds are planned against each region the floor tree is built from, the last circle is injected as an island, and emission goes through the same #575 frontier scheduler as the rough pass — byte-identical to plain offset when no area schedules three circles; walls stay contour-based either way. `cleanWallCorners` acts on the finish pass's **wall contour** rather than its outermost floor ring (issue #622): the control is labelled "Round wall corners" and promises to round the ring that defines the wall, but it used to be handed to the floor tree, whose root sits one stepover inside the wall path — so the outermost floor ring gained arcs and cleanup loops while the wall contour itself stayed a sharp mitered rectangle in every variant. The wall block now runs the same `planContourSmoothing` + `buildWallCornerCleanupContour` (`cleanup: 'all'`) construction the rough wall ring uses, so the arc's lost coverage is put back by traversing the exact sharp span; `pocketFinishLeftover.test.ts` measures 0.000000 in² leftover with the control on. Output is byte-identical with the control off, which is its saved-project state (`cleanWallCorners` has no default and no `normalize` backfill). Feed reduction is applied **once per finish band level, over the whole level** (issue #622): the band emits a floor block and then a wall block at the same Z, and classifying only the floor left every wall-finish and wall-cleanup cut at full feed however engaged it was -- 15.428 in of unpriced cutting on the shipped `purecutcnc` example, including two 1.81 in full-width slots in a corridor one tool diameter wide in tool-centre space, where no floor ring fits and the wall and island contours are the only passes. Running the classifier from before the floor loop gives the wall block the floor's kerf as prior context, so an ordinary wall contour one stepover outside the outermost floor ring still keeps full feed; both classifiers walk the stream forward-only, so the floor block's own output is unchanged (asserted in `engagementPocket.test.ts`)
- `carving.ts` — engrave / carve along a path (`'direct'` centreline or `'trochoidal'` overlapping-orbit slot)
- `drilling.ts` — drill-cycle generation, plus V-bit countersinking (issue #489), which derives its plunge depth from the requested mouth diameter and deliberately emits expanded moves rather than a canned cycle
- `edge.ts` — edge / profile-following cuts (outside/inside contour)
- `vcarve.ts` — V-bit carving via inset-contour offset stepping (`v_carve`, "V-Carve offset")
- `vcarveMedial/` — geometric medial-axis v-carve (`v_carve_medial`): Voronoi-of-boundary skeleton with exact clearances, corner tips, and contact-spread curve filtering (see its `INDEX.md`). Replaced the retired `v_carve_recursive` skeleton op (issue #279); saved projects using it migrate to this on load.
- `roughSurface.ts` — 3D rough clearing of an imported mesh. Dispatches on the declared pattern table like the other clearing kinds (issue #618): `offset`/`seeded_offset` cut concentric rings (seeded runs phase 1's seed stacks first, then rings offset around each extended seed island with the root's own island list restored), `parallel` cuts the level boundary contours first, then `buildPocketParallelSegments` scanlines — the pocket and surface_clean raster branches cut the same contours before their segments, and without them the silhouette keeps a scalloped ridge of standing stock that offset's outermost ring removes. Every branch keeps the per-level model protection: entries synthesize against the level's own regions at the global safe Z (no `withEntryStartZ`), and any link that stays at depth must pass that level's `safeLinkCheck` or the transition retracts
- `finishSurface.ts` — 3D finish pass dispatcher, and home to `criticalWaterlineFloorZs`. Waterline takes every Z carrying horizontal model surface as a critical level, because the contour right at the foot of a bump or the rim of a pocket is what keeps a thin band from being left between the lowest evenly-spaced stepdown and the actual floor. A flat region now has to be reachable to earn one (issue #685): this is #682's rule arriving at waterline, which does not go through `resolve3DSurfaceStepdown` and so never inherited it. On a quantized depth map every grey step is a plateau — the 291k-triangle relief behind #673 carried 431 of them a median 4.5 um apart, of which exactly one had the area to stand the 1.5875 mm ball on, and it was `effectiveBottom` and already excluded — so 431 critical levels joined 7 real stepdowns and the whole per-level pipeline ran 438 times. A Z keeps its level only when its flat-triangle area reaches `PI * toolRadius^2`, since a connected region holding a disc of radius R has area at least `PI * R^2` and the cutter tip can only sit at Z where a disc of its own radius is clear of material above it. Necessary and deliberately not sufficient, as in #682: the sum is over disconnected regions, so it keeps at least as many levels as a connected-component test would. Measured against `tool.radius` rather than `tool.radius + stockToLeaveRadial`, which is the choice that keeps the *more* levels of the two. Dropping a level only removes cutting motion, so it can only leave more material, and a Z that loses its critical level is left exactly as covered as any Z the mesh happens to carry no horizontal triangle at. Areas are merged on the same 1e-6 tolerance the horizontal test uses, which is not cosmetic — `Oldman-splash-final.camj` splits one physical floor across 151 Z values within 5e-6 of each other, and unmerged each piece would fall under the bound that the whole clears. That model is byte-identical across this change
- `finishSurfaceCleanup.ts` — cleanup-style 3D imported-mesh finishing that emits deepest retained wall/floor paths from rough-surface-style levels. Its floor honours `pocketPattern: 'seeded_offset'` (issue #609): seeds are planned against the residual floor regions the passes are built from, and each plan's extended island is injected into `buildCleanupFloorOffsetPasses`'s **inset recursion only** — the depth-0 boundary contours still come from the region's own outer and islands, which is what keeps the `suppressedBoundarySegments` contract intact (the contours suppression is applied to are byte-for-byte the unseeded ones, so a seed can never reinstate a boundary pass the wall finishing removed). The #576 leftover probe runs per level after the circles and passes; without it this file's own fixture strands 95 of 15,897 swept-coverage samples the plain offset pattern clears. Byte-identical to `offset` when no area schedules three circles
- `finishSurfaceParallel.ts` — parallel-line finish strategy
- `finishSurfaceWaterline.ts` — waterline (constant-Z) finish strategy with bounded, user-tunable adaptive shallow-slope refinement, mesh-safe projected bands, and terminal peak coverage. Decimates each mesh-slice contour before it reaches Clipper (issue #685). This file's `slicePolygonsToClipperPaths` is a second copy of the one #677 fixed in `surfaceStepdown3d.ts` and was still the pre-#674 version: full-density contours, one vertex per triangle-edge crossing, straight into a per-level `ClipperOffset.Execute` over a shadow that only ever grows. **The tolerance cannot be argued the way #677's was** — there the contours are a keep-out and thinning is paid for by expanding it, here the offset contours *are* the cut path — and the obvious replacement argument is also wrong: an outward offset at a concave corner is trimmed where two walls' offsets collide, so moving each wall by `d` moves that trim vertex along the bisector by `d / sin(theta / 2)`, and the emitted ring on `Oldman-splash-final.camj` moves by up to **129x** the tolerance. That excursion is a tool-*centre* artifact: at a trim vertex the cutter is tangent to both walls and has bottomed out, so what moves is where it stops, not what it removes. The bound that holds is on the shadow boundary the cutter stays tangent to, measured per boundary vertex at 5 um: p99 1.0x and worst 1.5x (7.4 um) on #673's relief, p99 1.0x and worst 5.0x (25.0 um) on Oldman, whose own scallop between adjacent passes is 21.7 um. Tightening to 2.5 um only reaches 5.1/17.8 um for 31% more time; loosening to 10 um jumps Oldman to 294 um, because features start collapsing rather than shifting. The constant is in mm and converted, because waterline runs in project units and `sliceDecimationTolerance`'s `clamp(r * 0.01, 0.002, 0.02)` would mean 51-508 um on an inch project. `DEFAULT_WATERLINE_SLICE_VERTEX_BUDGET` is the backstop, and it is **per-slice where roughing's is per-level and a cumulative one does not work at all**: the same real mesh spends 210,428 contour vertices in 6.8 s at one stepdown and 77,918 in 9.6 s at another, while a synthetic tube spends 224,007 in 198 s, so no cumulative threshold separates them — the vertex count of a single slice is what the superlinear offset is charged for, and it orders every fixture correctly. 12,000 is the geometric mid-point of the densest usable slice measured (8,334, real, 11.7 s CPU) and the lowest unusable one (16,001, 43.6-68.1 s); past it the operation refuses with `surface3dMeshTooDense`, every further slice returns empty so the level builds and the refinement wind down within one iteration, and nothing partial is emitted. Whole operation on the mesh behind #673, with the critical-floor rule above: **305.2 s wall / 211.2 s CPU -> 9.8 s / 12.5 s**, and neither half alone gets there — decimation alone leaves 107.4 s, the level rule alone leaves 59.9 s because the adaptive refinement then has real gaps to fill and its own cost goes 9.3 s -> 50.8 s on undecimated contours. The freeze itself is still #675's to remove. The adaptive refinement's own budget is priced before it is spent (issue #698): it used to be a single global ring counter of 1,000 enforced as rings were emitted, and since bands are processed from the top of the model down that made it first-come-first-served — whatever was highest got refined and everything below it got nothing. **A finer Adaptive spacing therefore produced a worse surface**, because halving the spacing doubled what each band consumed and the counter ran out higher up: flat area never cut on the hills fixture ran 14.3 / 14.3 / 16.2 / 33.9 % at 1.20 / 0.60 / 0.30 / 0.15 mm, and at 0.15 mm was no better than switching adaptive refinement off. The user-facing knob could not reach it either — raising *Max rings / band* from 96 to 512 produced byte-identical output at every spacing, because the global cap bound first. `generateProjectedWaterlineLevels` now runs **twice** behind a `dryRun` flag: the first pass charges every band and cap the area it would cover and emits nothing (the per-ring inward `ClipperOffset` loop, which is the expensive part, is skipped entirely), the second runs at the spacing that pricing chose. Repeating the walk is safe because it is deterministic — `processTipStack` branches only on sliced contours and `pathsAreRelatedForProjectedBand`, never on what was emitted — and the projected contours are memoized by Z across both passes, so each slice is paid for once and the slice vertex budget is charged exactly what a single pass charged it. The bound itself is stated as an **area ratio and not a ring count**, `WATERLINE_REFINEMENT_COVERAGE * footprintArea`, with the spacing cancelling out of it: covering an area at a spacing costs `area / spacing` of path, so `effective = requested * demand / budget` and the effective spacing is a *fixed multiple* of the requested one. That is what makes the response to Adaptive spacing monotone by construction rather than by tuning, and it is scale-free where a ring count is not — measured demand is 1.00x footprint on the hills fixture at both 0.30 mm and 0.15 mm, 3.43x at half the stepdown, and 4.56x on `Makera_Model.camj`. Cusp height goes as the *square* of spacing, which is what lets the coverage factor of 2 satisfy two criteria that pull against each other on Makera: coarsening 0.0100 -> 0.0228 in takes flat p90 from 0.992 mm to 0.064 mm and never-cut from 0.3 % to 0.0 % for 14.1 s against a shipped 8.6 s, where lifting the bound entirely costs 33.1 s and a factor of 1.5 lands on the 0.10 mm cusp budget with nothing to spare. *Max rings / band* is now a loop guard and not an allocator, and its default is a **reach** rather than a count (`ceil(diagonal / spacing)`), because a fixed 96 reproduced the same bug one level down — at half the spacing it reaches half as far, which is what kept the hills fixture at 14.6 % instead of 14.3 % at 0.15 mm once the global bound was fixed. When a bound does bite it says so out loud: `waterlineRefinementCoarsened` names the spacing actually machined, and `waterlineRefinementTruncated` reports the catastrophic 20,000-ring backstop, which sits an order of magnitude above both calibration fixtures. Both are real warnings and not `debug` lines, because the surface is not the one that was asked for and that has to be visible without Debug toolpath
- `surface.ts` — shared surface-toolpath helpers, and `surface_clean`'s own band generators. Its finish band mirrors the pocket's and carried the same issue #622 defect: `applyLevelFeed` ran at the end of the floor loop only, so the `wallStepLevels` block went out unclassified. Fixed the same way — one classification per band level
- `surfaceStepdown3d.ts` — shared imported-mesh stepdown resolver used by rough-surface and cleanup-surface operations. Decimates each mesh-slice contour before it reaches Clipper (issue #674): `sliceMeshAtZDetailed` emits one vertex per triangle-edge crossing, nothing downstream thinned it (`cleanClipperPath(path, 1.0)` drops vertices closer than 0.1 um at `DEFAULT_CLIPPER_SCALE`, i.e. nothing for a real mesh), and every one of them entered a single `ClipperOffset.Execute` per Z level whose cost is superlinear in the total — a detailed relief STL ran that offset for seconds per level until Firefox killed the script (#673). The keep-out is then pulled back by the error decimation **actually** introduced, not by the nominal tolerance: the margin is what makes the thinned contour provably contain the true cross-section, and it rides along on the sliver-cleanup erode/dilate already running on `clearablePaths` rather than at the `buildInsetRegions` call site, because `level.clearablePaths` reaches `roughSurface.ts` independently as the safe-link domain — a keep-out that shrank there would let the link planner rule a travel move safe across standing stock. That placement is also what keeps the fix from being a net loss: applying the margin as a `ClipperOffset` of its own cost 10 ms per level at 4,445 contour vertices, 107 ms at 13,690 and 810 ms at 35,308 — enough to make an ordinary model slower than before, and paid in full even on a mesh the budget was about to refuse. Folded into the existing open/close it costs nothing, and the envelope shift doubles to match because an erosion takes the outer-wall band in from both sides. Measuring the error rather than assuming it is what keeps a coarse or box-like model byte-identical: a rectangular cross-section has no vertex RDP can drop, reports zero, and skips the expansion, the envelope shift (`OUTER_WALL_MARGIN` is a single micron, so an assumed margin eats the whole outer-wall band and collapses the level) and the wider cleanup wall probe alike. `DEFAULT_SURFACE_3D_SLICE_VERTEX_BUDGET` is the backstop decimation cannot be: thinning buys a large constant factor but not a complexity class, so a level past 50,000 contour vertices refuses the whole operation with `surface3dMeshTooDense` rather than eating the tab. That 50,000 is sized against whole-operation cost on real relief meshes and deliberately **not** against #674's island-offset harness — 1,600 synthetic islands self-intersect far more per vertex than an actual relief contour, and taking the harness at face value gave a 20,000 budget that refused a plaque which resolves fine in 2 s. Measured through this resolver with both decimation and the budget disabled: 26,092 counted vertices took 3.0 s and 108,922 took 45.5 s, so 50,000 is about their geometric mid-point and interpolates to ~10 s. The freeze itself is #675's to remove; this only bounds it. A flat region also has to **earn** its own Z level (issue #682): every distinct Z carrying a horizontal triangle used to become a critical floor level, which is right for a genuine floor and catastrophic for a quantized depth map, where every grey step is a plateau — the reporter's 291k-triangle relief carried 431 of them, spaced 18 um apart under a 3.175 mm cutter, and `dedupeZLevelsDescending` merges only within `Z_TOLERANCE` (1e-6 mm), four orders of magnitude too tight to touch them. The whole per-level pipeline then ran 436 times for a 6-level stepdown, which is what #673's stack trace was really showing: `offsetPaths` was not slow, it was being entered 436 times. A Z now keeps its level only when its flat-triangle area reaches `PI * initialInset^2` — a connected region holding a disc of radius R has area at least `PI * R^2`, so a plateau under that bound provably cannot survive `buildInsetRegions` and the level could never have machined anything. The area is accumulated in the mesh-bounds loop that already visits every triangle, on transformed positions so it is in final project mm^2, and merged on `Z_TOLERANCE` so a plateau whose vertices differ in the last bits is not counted twice and dropped twice. It is deliberately *necessary* and not *sufficient*: the sum includes disconnected regions, so it keeps at least as many levels as a connected-component test would, and a long thin sliver that clears the bound is still left to the inset to reject. Dropping a level can only leave more material for the finish pass, never less, so nothing here can move a contour toward the model — 2 s CPU where the reporter's file took 123 s, and byte-identical on every fixture whose floors clear the bound. 2D subtract-feature bottoms are exempt; they are real machining floors whether or not the mesh has a plateau there
- `tabs.ts` — holding-tab generation on profile cuts
- `tabProfile.ts` — the pure Z profile a **smooth** tab imposes on one crossing: a normalized raised cosine with exact cut-Z at both footprint boundaries, exact `z_top` at the crossing centre, zero-slope joins at all three, and a sample count derived per crossing from a chord tolerance
- `tabSmoothing.ts` — chain-level smooth-tab motion. Measures each crossing in path distance along whole connected coplanar cut chains (rejoining a crossing that wraps a closed loop's seam) before sampling, so the emitted Z envelope depends on the crossing geometry rather than on how the source path was split into moves
- `trochoidalEdge.ts` — pure bounded overlapping-orbit sampling around a validated Edge Route guide; generation integration owns entry, fragments, and safety validation. Carries the folder's **two** trochoidal guards, which issue #662 separated because one flat 500,000-point ceiling had been answering both questions with the same message: `DEFAULT_TROCHOIDAL_POINT_BUDGET` is *how big a job may be* — 1,000,000, derived from a 248 MB memory target at the 248 B an emitted `ToolpathMove` was measured to retain, explicitly **not** a renderability figure (#664 measures the UI unusable near 249,663 moves and owns raising that separately) — while `MIN_ADVANCE_FRACTION` is *whether a parameter is defective*: an advance below `0.01 x D` re-cuts the same arc instead of progressing and is refused per fragment with `edgeTrochoidalAdvanceDegenerate` / `carveTrochoidalAdvanceDegenerate`, on guides far too short to reach any ceiling. Two boundaries are load-bearing there — exactly `0.01 x D` is the CAM panel's own minimum and must generate, so the cap measures the **requested** advance (deriving it from the ceil'd orbit count compared an integer with the real quotient it was rounded up from, and refused the panel minimum on any guide whose length was not a whole number of advances); and a single-orbit fragment is exempt, or every sub-millimetre tab fragment would refuse an ordinary operation
- `multiFeature.ts` — ops that span multiple features (e.g. combined clearing)

## Supporting modules
- `index.ts` — barrel export — add new files here when adding a strategy
- `types.ts` — shared toolpath types (segments, passes, parameters)
- `pocketPatterns.ts` — the one source of truth for pocket-pattern dispatch (issue #609). `OPERATION_PATTERN_SUPPORT` declares, per `OperationKind`, both the patterns the dropdown **offers** and the pattern each generator **runs** for every member of `PocketPattern` — so the CAM panel's option list and the generators' branches cannot disagree, which is how the same defect shipped three times (#579, #583, #609; the third emitted no floor motion and no warning at all). Consumed by `CAMPanel.tsx`'s `pattern` renderer, `operationFields.ts`'s applicability, the operation booklet's pattern rows, and the five generators. The two `Record<>`s are the ratchet: a new operation kind does not compile until it says whether it takes a pattern, and a fifth `PocketPattern` does not compile until every pattern-taking kind classifies it and `areaCoverage`'s `never` default is satisfied. `offered` is declared rather than derived because the two sets genuinely differ — a pocket resolves a stored `waterline` to its offset rings without offering waterline, and cleanup leaves a stored `waterline` inert — but every offered pattern must resolve to one that cuts. `rough_surface` joined the clearing set in #618: its stored `waterline` maps to the rings the kind hard-coded before it had a pattern row at all
- `clearingControls.ts` — the one source of truth for which clearing controls each level-based kind offers, with the recorded reason for every cell that does not (issue #616). `CLEARING_CONTROL_SUPPORT` declares, per `OperationKind`, either the six cells (`slotFeed`, `engagementMode`, `roundOutsideCorners`, `cleanWallCorners`, `cornerRelief`, `machiningOrder`) or the reason the kind clears nothing — same two-`Record<>` ratchet as the pattern table: a new kind and a new control each fail to compile until classified. Consumed by `operationFields.ts`'s predicates, `pocket.ts`'s `resolveSlotFeedScale`, and the operation booklet's setting rows. Two boundaries are deliberate: tangential S-links stay with `usesTangentLinks` above (pattern-dependent, already single-owner), and the edge routes' shared rounding/relief/order half stays inline at each consumer because they are not clearing kinds and sit outside #614's scope. The structural test in `operationFields.test.ts` fails when a kind's exposed set diverges from the declaration
- `feed.ts` — shared `effectiveFeed` helper: applies a move's `feedScale` (for slot-feed pocket cuts) to the cut feed, and returns the plunge feed unmodified for plunge moves; used by the postprocessor, booklet time estimator, simulation playback, and the live-feed readout
- `engagement.ts` — pure cutter-engagement estimator (issue #498): `SweptMaterialIndex` models each prior sweep as an exact tool-radius capsule in a centreline-supercover spatial index, and returns the leading-semicircle measure in uncut material in `[0, π]` through closed-form capsule-arc unions; `nominalEngagement` gives the analytic stepover wrap angle; `engagementFeedScale` maps engagement onto a fixed non-uniform rung ladder — fine near full feed, coarse near slot (issue #591; rounding down, conservative), and `EngagementFeedQuantizer` applies hysteresis plus a minimum fragment length so `feedScale` changes stay controller- and arc-fitting-friendly — a rise climbing more than one rung holds only a quarter of that length, and a sub-minimum stretch is priced at its longer neighbour's rung rather than always dragging the pair down (issue #594, bounded to one rung above its own verdict and to one minimum fragment of distance); `EngagementTelemetryAccumulator` folds distance-weighted samples into per-operation max/p95/distance-above-nominal telemetry. Degenerate input resolves toward more engagement. Tests: `engagement.test.ts`, `engagementOracle.test.ts`, `engagementPocket.test.ts`
- `entry.ts` — clearance-aware plunge, helical, and zig-zag ramp entry synthesis for pocket, surface-clean, and rough-surface clearing operations, including deterministic fallback warnings and plunge-feed limiting
- `geometry.ts` — toolpath-specific geometric helpers; owns the shared `DEFAULT_FLATTEN_*` sampling constants
- `trochoidalPath.ts` — shared trochoidal entry synthesis (helix/plunge points, move-count budgeting, and the per-operation point budget type) used by both Edge Route and Engrave
- `trochoidalLevelPaths.ts` — one generated orbit path shared by every Z level whose guide fragments the same way (issue #661). A deep cut with no tabs and no obstacles fragments identically at every level, so the generator ran once per level and stored a copy each time; the store keys on the **planner's output geometry** — the guide points plus the four generator parameters — rather than on the causes of fragmentation (tabs, obstacles, region masks), because `buildTrochoidalContour` is a pure function of exactly those and a key derived from causes has to be extended by hand every time a new input to fragmentation lands. Identical key therefore means provably identical output, and anything that changed the fragmentation is a different key by construction. Carried on `TrochoidalOperationBudget`, which #661 split into two accounts: `remainingMoves` charges every level, carries the pre-#661 arithmetic and is what refuses; `remainingPoints` charges a generated path once. Separating them is what lets #662 raise the emission ceiling without multiplying generation cost — charging generation *alone* took a 60 x 40 in outside route from an honest `edgeTrochoidalMoveBudget` refusal to 9,873,183 moves and ~2 GB of heap, because a reused array of points still describes motion the machine cuts at every level. **Sharing the path never shares the check**: the verification backstop and entry synthesis still run for every level at that level's own Z — `edge.ts` is where that is enforced and `trochoidalLevelSharing.test.ts` is what holds it there. A refused build is never cached (`move-budget` is a function of the remaining budget, not of the guide)
- `guideFragments.ts` — cyclic closed-guide fragmentation against pre-unioned keep-outs; preserves every safe span before entry-bearing motion is generated
- `tabs.ts` also owns the shared tab footprint geometry (`expandedTabFootprints`) and `applyEdgeRouteTabs`, the edge-route tab pass that deliberately returns trochoidal results untouched — see `planning/TROCHOIDAL_EDGE_DESIGN.md`
- `cornerRelief.ts` — corner relief (dogbone / T-bone / longest edge, `operation.cornerRelief`). Detects the convex corners of whatever region an operation clears — one sign test against which side of each loop the cleared material lies on, so the module never learns which operation kind called it — and emits relief as a **separate stepped pass appended after the main path**, not as a region modification (which silently no-ops: a radius-`r` lobe eroded by a radius-`r` cutter collapses to a discarded point) and not as an inline wall detour (which becomes a full-depth slot under the rough-big/finish-small workflow). The excursion ends where the cutter just *touches* the corner — the exact limit, not a conservative one — and the pass descends on the operation's own tool-centre path corner, which it does only when the emitted moves prove the operation cut there at or below the deepest relief level. That one guard covers `finishWalls`-off pockets, tab-covered outside corners, and trochoidal channels that never reach the contour, without naming any of them. Levels come from `min(tool.defaultStepdown, tool.maxCutDepth)`, never from `operation.stepdown` (hidden and stale on a finish pass)
- `offsetSmoothing.ts` — emit-time corner smoothing for the outer/wall clearing rings; shared by pocket + surface clearing when `roundOutsideCorners` is enabled. `planContourSmoothing` is the contour-level turn planner (issue #546): it analyses the whole closed ring, groups consecutive same-sign turning vertices into one geometric turn run, allocates straight-edge setback at contour scope rather than capping every corner at half its shorter edge, and returns the rounded contour plus per-turn metadata (source span, entry/exit tangent points, effective radius). `roundContourCorners` / `smoothClosedContours` / `cornerSmoothingRadius` are the points-only wrappers. Fails closed to the exact source geometry per turn (a starved shoulder that would put a tangent point inside its own run) and per contour (self-intersection, non-finite values). A run whose *path* — vertices **and segment midpoints** — already tracks a circle at least as broad as the request is left untouched; measuring vertices alone is meaningless, since any three points fit a circle exactly and a square's corners are concyclic. With `broadCorners` on (opt-in; off is today's exact output for every other caller) a turn the ordinary construction starves — a corner reached only through short tessellation edges, which comes out at a few percent of the request — is instead cut with one arc at the *full* radius across as many source vertices as that takes, via `broadCornerArc.ts`. Such a transition reports `cutsAcrossSource` and carries `spanPoints`, the geometry it cut away with the ordinary fillet spliced back in; a caller may only enable the option if it cleans that span, because the arc leaves a tip of stock behind. Rough/surface-pass islands are rounded the opposite way — via `jtRound` Clipper offsets (see `buildInsetRegions` island join), so the tool wraps convex island corners smoothly without gouging; the pocket finish wall's islands now go through this planner too (see the `pocket.ts` entry).
- `broadCornerArc.ts` — the full-radius corner arc for tessellated approaches (issue #546): rolls a circle of the requested radius into the corner until it jams against two *real* source edges, so both junctions are tangent-continuous by construction. This is the difference from the reverted attempt that fitted an arc to two shoulder lines read from further out — the approach curves, those lines leave the polyline, and the arc met the path at a kink on each side. A candidate is rejected unless both tangent points land inside their own segments, the sweep equals the source path's own accumulated turn, and nothing of the ring lies inside the circle; that last pair is what makes a ring narrower than two radii (a pocket's innermost slivers) decline and keep the geometry it already had
- `sweptCoverage.ts` — "has some other pass already removed this?" (issue #546): tool centrelines indexed into a uniform grid at one radius per cell. `pathIsCovered` asks whether a *line* lies inside the swept envelope; `sweptRegionIsCovered` asks whether everything a cutter following that line would *remove* is already gone, by rasterising the path's disc. The distinction is the whole point and cost a shipped defect to learn: a corner cleanup loop was dropped because the span it retraces was covered, while the loop also sweeps stock either side of that span, leaving a 0.21 x 0.05in patch 0.0057in proud. The area test needs no region check — callers hand it a loop already validated against the tool-centre domain, so every cell its disc reaches is material by construction. Every unanswerable question answers *no*. Used by Pocket to drop the redundant cleanup loops (which is what makes rounding cheaper than not rounding) and by `pocketClearance.test.ts`
- `wallCornerCleanup.ts` — pairs a rounded corner with an immediate same-Z cleanup of the stock its arc left behind, so a ring can be rounded where rounding otherwise removes coverage (issue #546). `buildWallCornerCleanupContour` emits the broad transition, returns to the transition entry along a domain-checked cubic, then traverses the span, restoring the coverage. Which corners get a loop is the `cleanup` option: a wall ring cleans `'all'` of them and traverses the exact sharp source span, restoring the legacy wall coverage; an interior ring cleans only `'cut-across'` — the transitions that cut across their own source vertices — and traverses their `spanPoints`, which keep the ordinary fillet so the loop rejoins on a curve rather than putting the bare apex back into the path. Both the arc and the return are sampled against the Pocket tool-centre domain; a corner failing either keeps its exact sharp geometry, so one reflex corner (whose arc always bulges off the ring into the wall) costs only itself and not the whole ring. `cleanupCount` reports how many corners were actually cleaned — 0 with planned transitions is what raises `pocketWallCornerCleanupFallback`.
- `seedClearing.ts` — seeded circle pocket clearing, phase 1 (issue #554), reached by `pocketPattern: 'seeded_offset'`. Clears each qualifying open area a region holds, largest first, with full circles grown from its clearance seed — `r0, r0 + stepover, …` while `r <= R_max` — then records a **virtual island** so phase 2 is today's ring tree unchanged, with its first pass landing one stepover outside it. `seedIslandRadius` (issue #576) is what that radius is, and it is not the last centreline: the cutter following that lap removed stock out to `lastRadius + toolRadius`, so injecting the centreline under-states what phase 1 emptied by a whole tool radius and the tree spends its innermost levels threading slivers through metal that is already gone — 7-18% of the seeded stream's cut length was removing nothing measurable. The island is therefore extended by `min(toolRadius - stockToLeaveRadial, 2*toolRadius - stepover)` less one chord tolerance. The first term is the stock boundary the stack reached; the second is the cap that keeps the first ring outside the island still overlapping what the stack swept, and it only binds above a 50% stepover — without it a ridge opens all the way round the seed and costs more to walk out than the graze rings the extension removed. The epsilon is the chord tolerance and not a magic number: the island is a tessellated polygon whose chords already sit one sagitta inside the true circle, so subtracting the same tolerance errs toward a hairline air pass rather than a hairline uncut ridge. The island is **hard for offset generation and soft for cleanup entry** — its interior is stock the stack already removed, so `seedLeftover.ts` may plunge into it. Whatever the enlarged island strands, `seedLeftover.ts` recovers. An area must schedule at least three circles: one or two circles split and truncate the later offset tree without clearing enough middle to earn that handoff, so the largest such remainder ends phase 1 and regular rings clear it. When `roundLinkCorners` is enabled, the direct transition into each qualifying circle goes through the same domain-checked tangent-S splice as offset rings, re-seaming the arrival circle and starting the next concentric circle at that new seam; a rejected candidate retains the direct link. Multi-area is a loop over the same query: each pass hands the disc it emptied to the next **as an island**, which is what keeps "never clipped" true beyond the first area — a later area's circles stand in uncut material rather than being trimmed against a cleared one. `seedSeparation` is what that exclusion costs: one tool radius for the reach of the last circle plus two stepovers for the ring tree to grow a pass out of each side of the gap, because leaving less lets two expanded islands meet and collapses the region between them, and no ring is then emitted in a gap the cutter was too small to have cleared on its own. That charge is measured from `lastRadius` and the extension adds at most a tool radius, so two *extended* islands are still two stepovers apart — asserted in `seedClearing.test.ts` rather than left to the reader. The exclusion polygon is deliberately coarse and circumscribed — every later search measures against every polygon before it, so a chord-tolerance one would make planning quadratic in the number of areas for no benefit. Two properties of the surrounding code are what make phase 1 free of Clipper and free of per-circle containment tests, and both are load-bearing: the regions handed to the ring builder are already in **tool-centre space** and `buildInsetRegions` expands islands by exactly `stepover`, so the injected circle needs no radius conversion; and `findLargestClearanceCircle` is refined by branch-and-bound until the bound closes, so it **under-reports** `R_max` and never over-reports, making one query the whole schedule. Core coverage does not depend on where the entry helix actually landed — the first circle sweeps `[r0 - toolRadius, r0 + toolRadius]`, so any `r0 <= toolRadius` reaches its own centre, which is the single rule `seedStartRadius` exists to guarantee (helix entries use the orbit radius, plunge and ramp start at the tool radius). `pocket.ts` owns the handoff and the ordering: phase 1 runs for **every** qualifying open area in the band before phase 2 starts, never interleaved area by area, so all the open middles are gone before the first ring is cut (phase 2 then re-orders the trees from wherever phase 1 left the tool, and reproduces the previous ordering exactly when there are no seeds). It builds the tree from the region **with** the seed island and then restores the root's original island list, because `cutOffsetRegionNode` cuts every island of the node it is cutting and phase 1 has already run that exact lap — stripping it drops the duplicate and returns the seed disc to the cleared domain that entry placement and tangential links are tested against. It also owns the leftover excursions: `planRegionSeedLeftovers`, `seedLeftoverCandidates` and `cutSeedLeftoverExcursions` live there, planned once per band with the tree and cut after the rings at every level
- `tangentLink.ts` — tangential ring-to-ring S-links (issue #545): `tangentSLink` builds the arc-line-arc path that departs the exit point along the previous ring's tangent and arrives on a vertex of the directed arrival ring along its tangent (the arrival vertex slides, the middle direction is swept, one arc radius is pinned to the floor, the closure solves the rest); `buildOffsetDomainCheck` is the cleared-domain predicate and `pocketTangentLinkOptions` the gate options (minRadius 0.25×D, length budget 2.5×D). No candidate fits ⇒ the straight link stays
- `linearMoveOptimization.ts` — pure generation-stage finalizer that removes zero-length duplicate moves and merges contiguous, direction-preserving, collinear XY moves; applied after tabs but before clamp warnings. Zero-length **rapids** are kept: they are each operation's entry-positioning marker for the postprocessor, not noise
- `arcReconstruction.ts` — recovers arcs/circles/beziers from flattened Clipper output: known-circle reconstruction, segment-preserving boolean reconstruction (annotation map), and the Clipper-offset simplification pipeline (Kasa fit + RDP). Also home to `simplifyClosedRing`, the closed-contour RDP `surfaceStepdown3d.ts` thins mesh slices with (issue #674) — same recursion, but measuring to the **segment** rather than to the infinite line so its reported deviation is a true Hausdorff bound, and falling back to the unsimplified ring rather than letting a small island collapse to zero area (that would delete a keep-out outright instead of moving it). `applyRDPToLineRuns` keeps the infinite-line distance it has always used, so 2.5D arc reconstruction is untouched
- `regions.ts` — region computation (which area belongs to which op): `buildRegionMask` composes region features into an ordered include/exclude mask in feature-tree order
- `regionDomain.ts` — typed region-mask → domain resolvers (`resolveRegionDomainArea` / `resolveRegionDomainCentre` / `resolveRegionDomainCurve`) shared by pocket, edge, carving, v-carve, surface, rest-region, parallel, and waterline finishing; the resolver table in `planning/REGION_FEATURE_SEMANTICS.md` owns which operation kind uses which resolver — do not re-implement mask composition per operation (issue #476)
- `toolpathDependencies.ts` — pure change-detection for the per-operation toolpath cache (issue #518, slice S1): `featureInstanceComputationEquals` (resolver-read instance fields only) and `diffToolpathInputs` (changed feature ids plus global invalidation for named-dimension, unit, model-asset, and feature-order changes). Not wired into `isCacheHit` yet — slice S2 owns that seam.
- `resolver.ts` — resolves features+operations into clipper input regions; V-carve accepts closed Subtract and Line features (S2), Pocket remains Subtract-only; Line paths use even-odd fill semantics for nested contour holes
- `restRegions.ts` — rest-machining region computation (what a prior tool missed)
- `silhouette.ts` — extracts 2D silhouette from 3D mesh for sketch projection
- `meshSlicing.ts` — slices a mesh at Z heights (used by surface strategies). Chain assembly walks **both** directions out of its start node (issue #689): the start is almost always mid-chain, and walking one way emitted an open contour two edges at a time, so a single ring split by a `ptKey` rounding tie became 12,333 chains for `stitchOpenChains` to reassemble at cubic cost — a hang, not a slowdown. Closed rings are unaffected; the fragmented output was already correct, only its cost was not
- `modelProtection.ts` — keeps cuts from violating the imported model
- `clamps.ts` — clamp clearance / avoidance regions
- `appendAll.ts` — `appendAll(target, source)`, the one way this folder splices a generated array onto another (issue #668). `target.push(...source)` passes one *argument* per element and throws `RangeError: Maximum call stack size exceeded` past the engine's argument limit — 124,413 on node v26.0.0, stack-size dependent — and generation routinely builds arrays past that: one trochoidal Edge Route fragment on a 60 x 40 in guide emits 204,696 cut moves, well inside the operation point budget and well past the limit. A scoped `no-restricted-syntax` rule in `eslint.config.js` keeps the spread from coming back here

## Tests
- `cornerRelief.test.ts` — corner relief asserted on **coverage of the emitted motion**, never on "the region changed": a swept-envelope model decides whether the corner wedge is cut, because a region-level assertion passes on the silent no-op that region union produces. The same model is run with the excursion ending at `1.2r` and must *fail*, which is what makes the touch-the-corner rule falsifiable rather than merely satisfied. Also covers the corner-qualification sets (convex boundary, reflex island, concave part outline), tessellated arcs and fillets producing nothing, `'none'`/legacy identity, the tool-derived stepdown, every guard path, the rough-big-then-finish-small workflow, `optimizeLinearMoves` preserving the out-and-back (it survives only because the collinear merge requires `dot > 0`), and mm/inch plus a cross-unit tool
- `linearMoveOptimization.test.ts` — zero-length removal, entry-marker (zero-length rapid) preservation, collinear merge, and boundary preservation
- `pocketClearance.test.ts` — corner smoothing may not leave stock (issue #546). Rasterises the material and tests every cell against the swept envelope of the emitted cuts, against a self-referential bar: rounding may not leave more uncleared material than not rounding, so whatever a round cutter can never enter cancels out. No other test here can see an option that quietly stops removing material, which is the one failure that reaches the workpiece. `pocket-rounded-corner-coverage.camj` is the fixture that caught a cleanup loop being dropped while it was still the only pass clearing part of the floor; three other pocket fixtures showed nothing
- `pocketFinishLeftover.test.ts` — the finish pass may not leave reachable island-corner stock (issue #550). Measures geometrically (morphological opening minus the swept envelope, via `src/test/pocketLeftover.ts`) on `pocket-finish-island-leftover.camj`: the rounded pass must come back clear (≤ 0.0002 in² vs the 0.015495 baseline — the defect was a missing island ring, not corner treatment), every emitted cut must clear every island by the finish tool radius (no pass may gouge), the unrounded pass must clear too (the missing ring, not the corner treatment, was the defect), and the island-free control must come back clear
- `sweptCoverage.test.ts` — the swept-envelope test (issue #546): the band boundary at exactly one radius, grid placement for far and negative coordinates and across cell rows, an excursion in the *interior* of a segment (both endpoints inside the band, the middle outside — only sampling finds it), and every fail-closed input
- `broadCornerArc.test.ts` — the full-radius corner search (issue #546): tangency to real source edges inside their own segments, the jam on a tessellated corner whose adjacent edge is a fraction of the request, walking further out when a short stub cannot hold the tangent point, declining a corridor narrower than two radii and a circle blocked by the far wall, sweep matching the source turn, scale/orientation equivalence, determinism, and an invariant fuzz over deterministically perturbed rings (never `Math.random`) asserting all of the above on every arc it returns
- `seedClearing.test.ts` — the two claims that reach the workpiece, plus the ones that let it ship. **No circle is ever clipped**: every emitted point tested against the region and its islands. **The seed disc is fully cleared**: rasterised against the swept envelope of the circles alone rather than inferred from the schedule, and re-run with the first radius pushed past the tool radius where it must *fail* — that falsification is what makes `r0 <= toolRadius` a tested rule instead of a comment. **Fewer than three circles ⇒ byte-identical to `offset`**, asserted move-for-move on a 653-move stream through real generation (a 20 mm pocket with a 6 mm tool leaves a 14 mm tool-centre region, enough for exactly two circles); a direct schedule fixture keeps the exact three-circle boundary. **Seed-circle S-links** compare enabled/disabled real streams, require direct radial links to be replaced by tessellated tangent paths, and rasterise the two swept envelopes to prove the replacement drops no coverage. **Several open areas in one region** each get their own seed, on a dumbbell whose two lobes are joined by a narrower neck: largest first, the second landing in the *other* lobe, and every later area's circles provably clear of every disc an earlier one emptied. **Phase 1 precedes phase 2 across areas**, on a two-pocket fixture, with phase-2 motion identified positively — a cut outside every seed's handoff radius can be neither a circle nor one of the radial links between circles. Also: the schedule stops at the first radius that does not fit, tessellation whose points lie exactly on the circle within the chord tolerance, the handoff sitting one stepover out, the seed cut once and not twice, and an island region neither seeding into the island nor crossing it
- `seedLeftover.ts` — the leftover probe behind the enlarged virtual island (issue #576). Extending the island is what deletes the graze rings and also what can strand stock, where the island merges with a wall or a real island and the ring tree collapses over a partial ring that would have cleared reachable metal. The recovery is deliberately not new geometry: **candidates are the contours of the tree built around the PRE-extension island** — the shipped behaviour — so every excursion is a *subpath of a path this code already emits*, and "cannot gouge" needs no fresh argument. A candidate vertex is needed when the disc a cutter standing on it would remove is not already inside the emitted swept envelope (`sweptCoverage.ts`, issue #546); everything in that disc is removable stock by construction, because the domain is the pocket already inset by `toolRadius + radialLeave`. Maximal runs of needed vertices become open excursions extended one vertex each side — that neighbour is *not* needed, so its centre sits in swept material and the excursion plunges through metal a previous pass removed, no helix or ramp budget. Only a candidate loop needed end to end is emitted closed, through the level's ordinary entry. Two constants carry the accuracy: candidates are resampled to `toolRadius/2` before probing (a mitered ring vertex can be 5 mm from its neighbour, which rounds a 6 mm sliver up to a 15 mm excursion), and the disc is sampled at `toolRadius/12` — a sixth resolves the wall ridges but leaves pinpoint cusps where an excursion's envelope meets a ring's. Each excursion is folded into the coverage before the next candidate is judged, or every level of the pre-extension tree emits its own pass over the same ridge. The coverage it judges against is built from the **smoothed** ring contours when rounding is on: raw contours claim the corner tips the emitter cuts short, which was worth six stranded samples on a rounded 70x50 pocket. Runs once per region, never per step level, and not at all when no island got an extension. Emitted after the level's rings, and only after — "already swept" is measured at that Z, which is what makes the plunge land in a slot rather than a stepdown of stock
- `seedLeftover.test.ts` — the probe's four claims (issue #576), over eight fixtures spanning seed-near-wall and seed-near-island at three stepovers and two radial leaves. **Nothing the `offset` pattern clears is left behind**: both streams rasterised at 0.25 mm, zero samples lost, not a tolerance. **The probe fires and the stream needs it** — four of the eight strand stock when the excursions are taken back out, which is what stops the matrix above from being satisfiable by a probe that never runs. Corner rounding is in the matrix because the probe judges coverage from ring contours and the emitter smooths them. **No excursion leaves the tool-centre domain or enters an island**, and **every open excursion starts and ends inside the swept envelope**, so its plunge goes through cleared metal. **The handoff got cheaper**: each fixture's stream is shorter than the recorded pre-#576 length for the same fixture (4-23%) — a path-length comparison on deterministic geometry, not a timing assertion. Plus the probe's degenerate answers: nothing emitted and no candidates both yield nothing, and an untouched candidate comes back whole and *closed*
- `seedScheduling.test.ts` — the seeded-pocket travel frontier (issue #575), on the committed `another-pocket-test.camj` fixture (eight seed stacks, one 21-branch offset tree): the first level's 29 contiguous runs must interleave in both directions (a ring between two stacks AND a stack between two rings — the old schedule satisfied only the first half), the first offset branch must be a seed-adjacent leaf and the longest (wall-side root) ring last, every seed stack must still emit its full laps, and the seeded stream must cover everything the plain offset pattern covers
- `tangentLink.test.ts` — the S-link solver (issue #545): tangency at both ends (departure and arrival), the lateral step between parallel rings, domain rejection, length budget, degenerates, determinism, and the option gating
- `tangentLinkIntegration.test.ts` — the wired feature on generated streams (issue #545): sharp link junctions disappear where the S fits, every remaining sharp junction is provably a gate rejection (re-run with the domain removed), stream contiguity (the reported disconnected-tangent regression), swept-coverage parity with legacy, wall/island domain respect, byte-identity for absent-vs-false / parallel / non-pocket ops, entry-handoff preservation, composition with `roundOutsideCorners`, slot-feed stamping on a genuinely slotting link, arc-run budget on a tracked fixture, and the load-time backfill path on a real fixture
- `tabProfile.test.ts` — exact endpoints/peak, zero-slope joins, monotonicity, truncated-crossing clamps, and a measured (not assumed) chord-error bound on the derived sample count
- `tabSmoothing.test.ts` — smooth-tab machine motion: rectangular tabs still step, a smooth crossing ramps with no vertical step, re-segmenting the same XY path tracks the same ideal curve, closed-loop seams stay one continuous crossing, overlapping rect/smooth tabs take the highest envelope, and every emitted segment is checked against an independently recomputed analytic envelope
- `trochoidalEdge.test.ts` — deterministic closed/open orbit sampling, seam closure, direction, normalized duplicate vertices, and fail-closed budgets, including the degeneracy cap's two boundaries at the sampler level: exactly `0.01 x D` generates on a guide that is deliberately not a whole number of advances, a hair under it refuses, and a single-orbit guide is exempt. Integrated cut-direction parity (trochoidal vs contour, inside and outside), circular/multi-target guides, and overlapping tabs live in `toolpaths.test.ts`
- `engagementOracle.test.ts` — validates `engagement.ts` against an **independently written algorithm**, not against its own contract: the production code unions closed-form angular intervals, the oracle brute-force samples the leading semicircle and tests each point by direct distance, so the two cannot share a bug. Covers the analytic wrap-angle identity, agreement on pseudo-random geometry with no closed form (deterministic LCG, never `Math.random`), the domain invariants (virgin material reports `π` not `0`; a kerf directly behind the cutter is excluded structurally by the leading-semicircle domain, so no own-trail heuristic is needed), and the concentric ring-corner spike — measured **approaching** the corner, because once the tool has turned the inner kerf lies abeam again and the spike vanishes. That last one is why the estimator exists: sharp ring corners reach ~168° against a ~78° straight run. Tolerances admit any *conservative* model of prior sweeps while still rejecting a wrong magnitude, sign, or domain
- `feed.test.ts` — shared effectiveFeed helper: cut/plunge/lead-in/lead-out move kinds, feedScale present/absent, and plunge ignores-feedScale invariance
- `entry.test.ts` — helix pitch/direction, region/island clearance, no-core diameter bounds, bottom flattening, ramp fallback, and plunge-feed limiting
- `geometry.test.ts` — shared nearest-neighbour ordering and squared-XY-distance behavior
- `guideFragments.test.ts` — cyclic span splitting across disjoint, seam-crossing, and concave forbidden regions
- `trochoidalLevelPaths.test.ts` — the fragmentation signature and the store (issue #661): the key separates on a 1e-9 coordinate nudge, a reordering, a dropped point, the `closed` flag, each of the four generator parameters, and `-0` against `0`; the store generates once across 15 identical levels, again for a genuinely different guide, and never caches a refused build
- `trochoidalLevelSharing.test.ts` — level sharing end to end (issue #661), driving `appendTrochoidalContoursAtLevels` directly. The emitted move stream and exported G-code are identical with the real store and with one that never reuses — a differential comparison rather than a golden, so it keeps meaning after #662 moves the ceiling; 15 no-tab levels generate one path where the never-reusing store generates 15; a tab crossing its `z_top` splits the signature and the levels either side cut different XY; two congruent spans in different places never collide; and a path shared from a shallower level still **fails closed** at the one deeper level where the injected safety predicate rejects it. The header records which mutation kills which assertion, including the two that were passing for the wrong reason until mutation testing said so
- `toolpaths.test.ts` — broad smoke tests across strategies
- `clearingControlEffects.test.ts` — clearing-control effect contract (issue #622): walks `CLEARING_CONTROL_SUPPORT` and asserts, per (kind, control) cell, that the effect lands where the control claims. `applies: false` cells are byte-identical across toggle on a fixture that could have expressed the control; `applies: true` cells verify the location of the change — `roundOutsideCorners` replaces sharp corners with arc segments, `cleanWallCorners` adds motion to the wall contour, `slotFeed` stamps exactly `pocketSlotFeedPercent / 100` on slotting cuts and never on plunges, `engagementMode` emits a multi-rung feed-scaling ladder, `machiningOrder` makes `feature_first` emit contiguous per-feature blocks, `cornerRelief` appends a dedicated stepped pass at concave corners. Cells that cannot be exercised on the available fixtures (mesh kinds for `cornerRelief`/`machiningOrder`, model-silhouette boundaries for `roundOutsideCorners`) are skipped with a written reason, never asserted as inert. Suite iterates the declaration so a new kind or control fails until classified — the same ratchet the declaration itself carries
- `regionDomain.test.ts` — the three resolvers: ordered include/exclude composition matching `buildRegionMask`, both-polarity dilation, null-mask parity, empty-domain handling, and the domain-constraint invariant; the waterline seam tests in `finishSurface.test.ts` pin the waterline generator to the shared resolver
- `resolverReadPath.test.ts` — resolved instance geometry and missing-definition behavior in toolpath resolution
- `toolpathDependencies.test.ts` — change-detection module (issue #518): display-only instance fields invalidate nothing, per-field and definition-level edits report exactly the affected ids, add/remove/reorder semantics, dimension/unit/model-asset global invalidation, and a 200-feature single-edit guard
- `vcarveLineResolver.test.ts` — S2 closed-Line V-carve resolver tests: single Line, open-Line rejection, nested even-odd holes, disjoint Lines, mixed Subtract + Line, Subtract-only regression
- `clamps.test.ts` — clamp collision warnings, rapid auto-lift, per-move collision tagging
- `drillingRetract.test.ts` — drilling retract-plane safety (issues #479/#481): `retractHeight` is a distance above the material surface since format 3.1, resolved against that surface; negative distances are clamped back to it and warned about rather than rapid-plunging into the part. Asserts one invariant over both the toolpath and the posted program — every rapid endpoint below the surface must sit where a fed move already cut — which admits G83 re-entry and helical bore retracts while rejecting a traverse or descent into uncut stock. Also covers origin-independence of the stored distance, the canned-cycle `R` plane, and the safe-Z entry marker that keeps the first hole's traverse off the retract plane
- `carving.test.ts` — direct-mode regression, open/closed guide orbits, swept envelope, region polarity, fail-closed width/V-bit/budget guards, and open-guide cut-direction parity
- `drillingCountersink.test.ts` — V-bit countersinking (issue #489): the derived plunge depth `D / (2·tan(θ/2))` measured from each target's own top face for 90° and 82° cutters, the centre-locked plunge/retract shape, every fail-closed rejection (no V-bit, unusable angle, missing diameter, mouth wider than the cutter, plunge past max cut depth, mouth no wider than the hole, non-circular target), and the two non-regression claims: countersink output posts as linear motion on a machine that *does* emit G81, and legacy drilling operations are byte-identical with the new field present
- `camOperationSmoke.test.ts` --- per-operation-kind smoke covering every kind: pocket parallel/waterline patterns, drill-type differentiation (simple/peck/dwell/chip_breaking), thin ops (v_carve, surface_clean, follow_line, v_carve_medial; closed-Line V-carve smoke), edge route inside/outside, and the three 3D surface kinds via the `test-fixtures/*.camj` meshes; also documents the stock-target resolver gap. Its shared `postToolpath` helper runs `optimizeLinearMoves` before posting — matching the real `useToolpathGeneration` pipeline — and asserts the #467 entry invariant on the emitted G-code, so every present and future smoke test gets that check for free (issue #470)
- `pocketPatterns.test.ts` — the table's structure and, more to the point, the claim it makes (issue #609): every `(kind, pattern)` pair the table **offers** is generated for real on a fixture that pattern should cut and must emit a non-empty floor, with walls off so a wall pass cannot hide an empty floor — which is exactly how #609 stayed invisible. Restoring the pre-fix `finish_surface_cleanup` + `seeded_offset` behaviour fails this on the emitted stream, not on the table's own fields
- `surfaceStepdown3dDecimation.test.ts` — mesh-slice decimation and the slice vertex budget (issue #674). A tapered relief tube whose cross-section is a radial modulation of a circle, so every slice is one closed ring of known size and each level's ring strictly contains the ones above it — the level's own slice, not the protection inherited from above, is what has to contain the model. Four claims: the thinned keep-out **contains** the undecimated one (no part of the true cross-section may fall inside `clearablePaths`, which is both the cut domain and the safe-link domain — measured 0, 0 and 2.6e-7 mm2, against 0.849 mm2 with the margin deleted); a coarse box pays **no** margin at all, which is what keeps existing programs byte-identical; a mesh past the vertex budget refuses with `surface3dMeshTooDense` and zero moves while the *same vertex count* smooth enough to thin resolves normally; and the cost ratio against an undecimable reference. That reference is the same mesh with a +/- 0.1 mm sawtooth, so RDP keeps every ring vertex — it is not perfectly invariant and the comment records why (any triangulated quad wall leaves half its slice points collinear to within 1e-4 mm, so the reference thins 2x whatever the ring shape), which makes the ratio understate the regression rather than drift
- `finishSurfaceWaterlineBudget.test.ts` — the adaptive-refinement budget (issue #698), on the synthetic hills-and-flats fixture in `src/test/waterlineHillsFixture.ts` at a coarse mesh so it can be generated four times. Four claims: flat area never cut is non-increasing as Adaptive spacing falls across 1.20 / 0.60 / 0.30 / 0.15 mm, which is the acceptance criterion; the *share* of refinement landing in the lowest quarter of the Z range does not collapse as the spacing gets finer, which is the defect stated as a distribution rather than as an outcome; a project inside its budget raises neither budget warning; and an over-budget one is coarsened **proportionally** — halving the requested spacing halves the effective one to within 5 %, which is the property that makes the parameter monotone. Coverage is measured in XY, matching `scripts/waterline-coverage-probe.ts`'s `miss%`, because never-cut is about stock the pass never reaches. Every assertion is mutation-verified and the results are recorded: restoring the 1,000-ring top-down counter takes 0.15 mm from 7.0 % never cut to 12.2 % and the low-band share from 56.3 % to 38.2 %, and clamping the effective spacing to an absolute 0.4 mm breaks the proportionality ratio from 0.50 to 0.65. The first test also asserts the coarsest run still leaves uncut flat area, so the monotonicity claim cannot go vacuous if the fixture is ever changed — an earlier smaller fixture reached full coverage and proved nothing. The residual floor it does leave is the flat plateau tops, which no spacing reaches and which are issue #699, not this one
- `finishSurfaceWaterlineDecimation.test.ts` — waterline slice decimation, the per-slice vertex budget, and the reachable-floor rule (issue #685). Same tapered relief tube as the roughing decimation suite, and the same sawtooth reference. Six claims: the thinned **surface** — the shadow boundary the cutter stays tangent to, not the emitted ring, which moves much further at a concave trim vertex — stays inside a fixed 0.01 mm of the undecimated one in both directions, stated as Clipper booleans on the eroded and dilated true shadow; a coarse box cross-section is charged zero and keeps its enclosed area, which is what keeps an existing box-like program unchanged (it does lose the collinear mid-wall point every triangulated quad leaves, at exactly zero deviation); one genuine floor earns a level while 200 sub-cutter plateaus earn none, and the same staircase under a cutter small enough to stand on one step keeps all 200, which pins the rule to area rather than to plateau count; 25 strips of one plane spread over 1e-8 mm of Z merge to a single floor; a mesh past the budget refuses with zero moves while the *same point count* smooth enough to thin resolves; and the cost ratio against the undecimable reference. **The surface bound is deliberately absolute rather than a multiple of the tolerance** — written the second way it scales with the constant it guards and survives any mutation of it, which the first draft of this test did. All four mutations are recorded and were run: tolerance x10 fails containment (1.07 mm2 escapes), tolerance to 0 fails three tests, the budget x1000 stops the refusal, and a zero floor-area bound keeps every plateau. The budget fixture is 13,000 points and not 14,000, which hangs this generator on `main` as well — a ring sampled at an exact multiple of its own 8-lobe period degenerates somewhere in the offset while 13,900 and 14,100 both resolve in under a second (issue #689)
- `surfaceStepdown3dFloorLevels.test.ts` — which flat regions earn their own rough level (issue #682). A block with one square recess whose floor sits off the stepdown grid, built twice either side of the exact boundary the rule draws (`sqrt(PI) * radius` = 2.8135 mm of square side for the 3.175 mm tool): 2.7 mm must lose the level, 2.95 mm must keep it *and* carry `isCriticalFloorLevel`, since that flag is what makes 3D cleanup emit the floor pass. The same fixture with 0.2 mm of radial stock to leave loses it again, pinning the bound to `initialInset` rather than to bare tool radius, and a variant whose floor is split across two Z values 5e-7 mm apart keeps it, pinning the `Z_TOLERANCE` merge. Then a 12x12 quantized depth map — 144 plateaus of 4 mm^2 each over one genuine 576 mm^2 floor at z=0 — which must collapse to the stepdown's own level count with z=0 as the only critical floor. The pocket block's own three floors (z=0, 2, 4) are guarded in `roughSurface.test.ts` instead, on the committed fixture the acceptance names
- `roughSurface.test.ts` / `finishSurface.test.ts` / `finishSurfaceCleanup.test.ts` / `meshSlicing.test.ts` / `vcarveMedial/vcarveMedial.test.ts` — strategy-specific. `finishSurfaceCleanup.test.ts` also owns the seeded-floor claims (issue #609): the emitted stream contains full tessellated circles at `r0 = toolRadius` one stepover apart (a regular polygon's constant chord and constant turn is a shape nothing else this generator emits), it loses none of the swept coverage the `offset` pattern achieves, its no-seed fallback is byte-identical to `offset`, and every wall segment is still cut exactly once with seeding on
- `surfaceOperationValidation.test.ts` — real cone and hard-edge fixture matrix across Rough, Parallel, Waterline, and Cleanup, using an independently rasterized target surface plus swept-cutter simulation to validate stock, peak coverage, projected passes, determinism, and stable-interior gouging
- `pocketTessellationConsistency.test.ts` — regression for circle/arc sampling consistency (issue #359): full-circle and broken-circle pockets must have identical chord sagitta
- `arcReconstruction.test.ts` — direct partial-run arc-search coverage: greedy longest valid runs, sweep direction, and the bounded large non-fitting path (issue #369)
- additional arc-reconstruction coverage lives with its store-level callers: `store/helpers/offsetSimplify.test.ts` (offset simplification) and `store/second_cut_test.ts` (segment-preserving boolean reconstruction)

- `appendAll.test.ts` — appends 200,000 elements, a size checked against the spread limit measured on the running engine (`src/test/spreadLimit.ts`) so the fixture cannot decay into an array a spread would have handled anyway; plus order, object identity, empty source, and a non-array iterable
- `trochoidalPointBudget.test.ts` — the two guards issue #662 separated, asserting **which** one fires rather than only that one did: the 400 x 300 mm / 2 mm stepdown / 20 mm deep case that used to be refused now generates and stays under the ceiling; a bigger job refuses as a ceiling breach and not as a bad parameter; a degenerate advance on a 50 mm part no ceiling would ever catch refuses by naming the advance; and the CAM panel's smallest advance (`0.01 x D`) cuts on ordinary parts while a hair under it does not. The file header lists what each mutation kills, and each was verified by breaking the code on purpose
- `edgeTrochoidalEmission.test.ts` — the issue #668 regression through the real generator: a 60 x 40 in outside trochoidal route emits one fragment of 204,696 cut moves, which must be generated rather than thrown on or refused. Asserts against the measured spread limit rather than a constant, so a fixture that stopped exceeding it fails loudly instead of passing quietly
- `trochoidal249kFixture.test.ts` — calibration guard on the `trochoidal-249k.camj` render fixture (issue #664). The 2 x 2 in trochoidal edge route must keep emitting **exactly** 249,663 moves, in the same cut/lead_in/rapid/plunge mix, over 15 flat levels of 16,536 cut moves, **with no warning** — the last one being the defect itself, since a user at this move count is told nothing today. Exact rather than a range on purpose: every figure in #664, the render profile taken against it, and phase 3's advisory threshold are calibrated on this number, so a change that moves it must re-measure the issue rather than widen the bound. The header records which mutation kills which assertion; all three were verified by breaking the code (and the fixture) on purpose

## Adding a new strategy
1. New file `myStrategy.ts` exporting a generator function.
2. Add `export * from './myStrategy'` to `index.ts`.
3. Add a sibling `myStrategy.test.ts` with unit tests (required by `AGENTS.md`).
4. Update this INDEX.
