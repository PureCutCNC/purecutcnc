# Feature References Management Ledger

Integration branch: **`feature-references-v2`** (off current `main` since the 2026-06-19 port). The original `feature-references` branch is **superseded** — it was built on a pre-core-arch-refactor base; slices 01–06 were ported onto `main`'s new store architecture in `feature-references-v2`. All further work (creation-gap slice, 07, 08, final PR) happens on `feature-references-v2`. See the port entry in the Management Log and `planning/FEATURE_REFERENCES_PORT_Plan.md`.

Management rule: keep this ledger and `planning/FEATURE_REFERENCES_Plan.md` current before or in the same management commit as each accepted slice merge. Push the integration branch after every accepted slice merge.

| Slice | Branch | Worktree | Status | Owner | Base Commit | Result Commit | Verification | Merge Commit | Notes |
|---|---|---|---|---|---|---|---|---|---|
| 01 Model/versioning/migration | `feature-references-01-model-migration` | `/Users/frankp/Projects/worktrees/purecutcnc/feature-references-01-model-migration` | Merged | TBD | Integration branch at assignment time | `3d90a48` | `npx tsx src/store/featureReferencesMigration.test.ts`; `npx tsx src/store/projectStoreTransform.test.ts`; `npx tsc -p tsconfig.app.json`; quick duplicate-ID normalization check; `npm run build` pass. | `69e14aa` | Accepted after fix 2; branch returned as hyphenated because `feature-references/...` cannot coexist with integration branch ref |
| 02 Resolver contract | `feature-references-02-resolver` | `/Users/frankp/Projects/worktrees/purecutcnc/feature-references-02-resolver` | Merged | TBD | `b0fa801` | `15782d0` | `npx tsx src/store/featureResolver.test.ts`; quick explicit-missing-definition fallback check; `npm run build` pass. | `fa9bc45` | Accepted after fix for explicit `definitionId` missing-definition fallback |
| 03 Core read-path migration | `feature-references-03-read-paths` | `/Users/frankp/Projects/worktrees/purecutcnc/feature-references-03-read-paths` | Merged | TBD | `e4aab26` | `5780127` | `npx tsx src/components/canvas/hitTest.test.ts`; `npx tsx src/engine/toolpaths/resolverReadPath.test.ts`; `npm run build` pass. | `6f0506f` | Accepted after revised fix preserves explicit missing-definition skip/null behavior while keeping transitional raw fallback |
| 04 Transform commands | `feature-references-04-transform-commands` | `/Users/frankp/Projects/worktrees/purecutcnc/feature-references-04-transform-commands` | Merged | TBD | `7a7189f` | `4d3a56b` | `npx tsx src/store/instanceTransforms.test.ts` (21); `npx tsx src/store/projectStoreTransform.test.ts` (12); `npm run build` (tsc -b + npm test + vite) pass. | `278643c` | Accepted: move/rotate/resize/mirror/align/distribute + copy builders compose onto `instance.transform` via new `src/store/helpers/instanceTransforms.ts`; compatibility `sketch.profile` baking preserved; transform survives normalize via spreads; matrix convention matches resolver `applyMatrixToPoint` |
| 05 Definition editing | `feature-references-05-definition-editing` | `/Users/frankp/Projects/worktrees/purecutcnc/feature-references-05-definition-editing` | Merged | TBD | `c070fa6` | `6dcea90` | `npx tsx src/store/definitionEditing.test.ts` (13); `npx tsx src/store/featureResolver.test.ts` (25, no regressions); `npm run build` (tsc -b + npm test 29/29 + vite) pass. | `cb8a25a` | Accepted: 6 edit actions + applyProfileBreak route to `definition.profile` then `rebakeAllInstances`; new `featureDefinitions.ts` (rebake/clone/makeUnique); edit-sketch profile swap via `selectionSlice` enter/apply (no canvas change). Risks tracked for slice 08 browser validation (see management log) |
| 06 Snapshot operations | `feature-references-06-snapshot-ops` | `/Users/frankp/Projects/worktrees/purecutcnc/feature-references-06-snapshot-ops` | Merged | TBD | `5388ee4` | `d12e36d` | `npx tsx src/store/snapshotOps.test.ts` (13); `openProfileJoin.test.ts`, `second_cut_test.ts` (no regressions); `npm run build` (tsc -b + npm test 30 files + vite) pass. | `e48c1dc` | Accepted (last slice on stale base): join/cut/offset/merge resolve inputs via `resolveFeatureInstances`; outputs = fresh definition (`createSnapshotDefinition`, `nextUniqueGeneratedId 'def-'`) + identity instance; merge-then-GC ordering correct; `keepOriginals` both modes + sibling isolation + undo covered. **Port note:** `syncIdCounter` does not scan `featureDefinitions`, so the id-counter invariant isn't maintained for `def-` ids (no demonstrated collision — `f-` requested before `def-` — but fragile; fix in the port on main's id system) |
| 06.5 Creation definitions | `feature-references-065-creation-definitions` | `/Users/frankp/Projects/worktrees/purecutcnc/feature-references-065-creation-definitions` | Merged | TBD | `4841867` | `d856d9b` | `npx tsx` FR suites (creationDefinitions 10, snapshotOps 13, featureResolver 25, instanceTransforms, definitionEditing 13, camj, migration 14 — all pass); `npm run build` (tsc -b + full test + vite) green on slice and on merged integration. | `9218c30` | Accepted: new shared `createDefinitionForFeature` helper mints a definition (`f-` id) + identity transform; wired into `addFeature` chokepoint, `importShapes` (per imported feature), and `.camj` `mergeCamjFolders` (collision-safe source-definition remap via stateful `nextId('f-')` + mint for definition-less). Idempotent (skips features with explicit `definitionId`). Verified no duplicate-definition-id in the import `.map` — `genId` advances a module-level counter so each call is unique. Creation-gap CLOSED; unblocks slice 07 |
| 07 UI workflow | `feature-references-07-ui-workflow` | `/Users/frankp/Projects/worktrees/purecutcnc/feature-references-07-ui-workflow` | Merged | TBD | `3111192` | `7d26bf6` (impl) + `b3ebfaa` (mgmt icon/CSS fix-up) | All FR `npx tsx` suites pass (duplicateReference + 6 existing, no regressions); `npm run build` (tsc -b + full test + vite) green on slice and on merged integration. | `13c9aa6` | Accepted with one management fix-up. Impl: `copyMode` in `ProjectMeta` (default reference, normalized, undoable setter); `duplicateAsReference` (same definitionId, profile baked from `resolveProfile`, no double-translate) + `duplicateIndependent` (clone definition, merged via `extractClonedDefinitions`); default copy path follows `copyMode`, context items force a mode via `PendingMoveTool.copyMode`; 4 context-menu items (Make Unique reuses existing store action); linked badge via `getInstanceIdsForDefinition`; Properties Shape-vs-Instance grouping. Mgmt fix-up `b3ebfaa`: linked badge `<Icon id="link">` had no icon (icons.svg is generated from `src/assets/icons.camj` per ARCH §5) and no CSS → blank box; added a `link` icon (two overlapping rings) to `icons.camj`, regenerated `icons.svg` via sync-icons, added `.tree-linked-badge`/`.tree-icon--link` CSS. Verified every other icon regenerated byte-identical (icons.svg diff = +4 lines). Browser/tablet validation deferred to slice 08 |
| 09 Edit Sketch in place | `feature-references-09-edit-in-place` | `/Users/frankp/Projects/worktrees/purecutcnc/feature-references-09-edit-in-place` | Merged | TBD | `8cb1eb9` | `a725d2a` | FR `npx tsx` suites (editInPlace 11, definitionEditing 11, duplicateReference, featureResolver 25, instanceTransforms, snapshotOps 13, creationDefinitions 10, migration 14 — all pass); `npm run build` green on slice and merged integration. | `285c5ca` | Accepted: `enterSketchEdit` no longer forces identity; `syncEditedFeatureDefinition` inverse-transforms the edited world-space profile to definition-local via new `invertMatrix` + `transformProfileAffine` before writing; dead `editingFeatureId` path removed (RebakeOptions, rebake branch, 5 callers, projectStore). Verified `invertMatrix` formula correct; removed `definitionEditing` tests #4/#5 were obsolete (asserted the removed identity-force) — legitimately replaced by `editInPlace.test.ts`. Covers both user symptoms (transformed-instance edit + make-unique-then-edit). Browser validation pending |
| 10 Linked constraint re-solve | `feature-references-10-linked-constraint-resolve` | `/Users/frankp/Projects/worktrees/purecutcnc/feature-references-10-linked-constraint-resolve` | Merged | TBD | `915fb0c` | `7ba2d04` | `linkedConstraintResolve.test.ts` 3/3; `constraintSolver.test.ts` pass; FR suites (definitionEditing 11, editInPlace 11, featureResolver 25, duplicateReference, snapshotOps 13, migration 14) pass; `npm run build` green on slice and merged integration. | `TBD` | Accepted: in `syncEditedFeatureDefinition`, after `rebakeAllInstances`, seed `propagateConstraintsOnTranslate` with `{0,0}` for ALL instance ids of the definition (`getInstanceIdsForDefinition`) → triggers the dependency-graph re-solve (`propagateRigidTransforms`→`solveFeatureTranslation`) against rebaked geometry, then `validateConstraintsOnFeature`. Mirrors the direct-edit repositioner (STEP-1 finding); reuses existing helpers, no new solver; only the choke point touched so all edit actions covered. Idempotent (no-drift test). Browser validation pending. Merge `5ab597c` |
| 08 Integration verification | `feature-references-v2` | `/Users/frankp/Projects/worktrees/purecutcnc/feature-references-v2` | In progress | Management | `10b925d` | n/a | Browser pass PASS (see log 2026-06-19); full `npm run build` green at merge. Tablet validation + final user sign-off still pending. | TBD | Browser validation done by management on the running app. Remaining: user tablet validation, then final PR to `main`. Do not archive plan until user confirms |

## Management Log

- 2026-06-18: Plan moved to In progress on integration branch `feature-references`; ledger and first handoff drafted.
- 2026-06-18: Slice 01 Model/versioning/migration marked In progress.
- 2026-06-18: Slice 01 report reviewed at commit `c589d0c`. Focused tests and `npm run build` pass, but management review found a blocker: definitions are built before existing legacy feature normalizations, so definition data can disagree with normalized features. Marked Needs changes.
- 2026-06-18: Slice 01 revised report reviewed at commit `6239123`. Previous normalization-order blocker is fixed and focused tests, `npx tsc -p tsconfig.app.json`, and `npm run build` pass. Still marked Needs changes because definitions are created before `dedupeProjectIds`; a legacy duplicate feature ID normalizes to feature IDs `f-0001` and `f0002` but only creates definition `f-0001`.
- 2026-06-18: Slice 01 fix 2 reviewed at commit `3d90a48`. Focused tests, duplicate-ID normalization check, `npx tsc -p tsconfig.app.json`, and `npm run build` pass. Accepted and merged into integration.
- 2026-06-18: Slice 02 Resolver contract handoff prepared from integration commit `b0fa801` and marked Assigned.
- 2026-06-18: Slice 02 report reviewed at commit `673cb16`. Focused resolver tests and `npm run build` pass, but management review found a blocker: a feature row with an explicit missing `definitionId` falls back to `feature.id` and can resolve a stale/wrong definition. Marked Needs changes.
- 2026-06-18: Slice 02 fix reviewed at commit `15782d0`. Focused resolver tests, explicit missing-definition fallback check, and `npm run build` pass. Accepted and merged into integration.
- 2026-06-18: Slice 03 Core read-path migration handoff prepared from integration commit `e4aab26` and marked Assigned.
- 2026-06-18: Slice 03 report reviewed at commit `610822a`. Focused hit-test and resolver read-path tests pass, but management review found a blocker: the toolpath read path uses raw compatibility geometry when `resolveFeatureInstance()` returns null for an explicit missing `definitionId`, contrary to the slice 02 resolver contract. Marked Needs changes.
- 2026-06-18: Slice 03 revised report reviewed at commit `5780127`. Focused hit-test and resolver read-path tests pass, `npm run build` passes, and the explicit missing-definition read paths now skip/null without raw fallback while transitional rows still fall back. Accepted and merged into integration.
- 2026-06-18: Slice 04 Transform commands handoff prepared from integration commit `946e3b3` and marked Assigned. Scope: route move/rotate/resize/mirror/align/distribute onto `instance.transform` (new `src/store/helpers/instanceTransforms.ts` matrix helpers), dual-writing the compatibility `sketch.profile` so un-migrated direct readers stay correct; preserve uniform circle resize and identity-migrated behavior; backdrop/stock transforms and `Project.features` shape change out of scope.
- 2026-06-18: Slice 04 report reviewed at commit `4d3a56b` (based on integration `7a7189f`). Diff in scope (new `instanceTransforms.ts` + tests, `projectStore.ts`, `pendingCompletionSlice.ts`, transform test, `INDEX.md`). Verified: matrix builders/compose use the same column-vector convention as resolver `applyMatrixToPoint`; mirror/rotate/scale deltas geometrically correct; `transform` runtime field survives `normalizeProject` via spreads; definitions built from original profile before transforms so resolved geometry equals baked compatibility profile for identity-migrated rows. Focused tests (21 + 12) and `npm run build` pass. Accepted and merged into integration (merge `278643c`, ledger hash `c070fa6`).
- 2026-06-18: Slice 05 Definition editing handoff prepared from integration commit `c070fa6` and marked Assigned. User chose the plan-faithful approach (no shortcuts): sketch edit opens the canonical untransformed definition (definition-local space) even for transformed instances; all profile/circle edits mutate the shared `FeatureDefinition` and re-bake every referencing instance's compatibility `sketch.profile` through its transform; adding a point to a circle converts the shared definition kind; add a `makeUnique(instanceId)` store helper (clone definition + repoint), UI wiring deferred to slice 07. Larger slice — touches `SketchCanvas.tsx` edit-entry plus the store.
- 2026-06-18: Slice 05 report reviewed at commit `6dcea90` (based on integration `c070fa6`). Diff in scope (new `featureDefinitions.ts` + 13 tests, `projectStore.ts` 6 edit actions + `applyProfileBreak` routed to definitions, `selectionSlice.ts` enter/apply sketch-edit profile swap, `types.ts` makeUnique). Note: implemented without `SketchCanvas.tsx` changes — definition-local rendering achieved via a profile swap in `selectionSlice` enter/apply. Verified: enter/apply/cancel lifecycle correct (snapshot captures pre-swap world-space; round-trip works with/without edits and for transformed instances); `rebakeAllInstances` reuses `resolveProfile`; origin/orientationAngle reset to 0 matches resolver `resolveSketch` convention. The large `projectStore.ts` churn is mostly re-indentation (moveAnchor logic moved out of `.map`). 13 focused + 25 resolver tests + `npm run build` (29/29) pass. Accepted and merged into integration. **Risks to validate in slice 08 (browser):** (a) features with non-zero `sketch.origin`/`orientationAngle` — `src/engine/csg.ts` and `transformFeatureProfile` read these raw; rebake forces 0, fine for normal (0/0) features but unverified for stock-source/some imports; (b) `enterStockSketchEdit` does not swap to definition-local (agent known gap); (c) transformed linked instance edited through the full store-action + canvas pipeline not yet browser-validated; (d) `makeUnique` uses a module-level counter + `def-clone-N` id namespace separate from the project id system (collision-guarded but slightly inconsistent).
- 2026-06-19: Slice 06 Snapshot operations handoff prepared from integration commit `ef95941` and marked Assigned. Plan-faithful (no shortcuts): join/cut/offset (and shared boolean combine) resolve inputs via the resolver and create a fresh `FeatureDefinition` + identity-transform `FeatureInstance` per result (new `createSnapshotDefinition` helper); preserve `keepOriginals`; GC a consumed instance's definition when it loses its last reference; leave linked siblings byte-identical. **Open question raised to management (creation-definition gap):** no runtime creation path (`createDerivedFeature`, plain draw, text, import) currently mints a `FeatureDefinition` — new features rely on the slice-03 raw fallback. Slice 07 (Duplicate as Reference) needs source features to HAVE a definition. Recommendation: insert a small "creation paths create definitions" slice (covering draw/text/import) before slice 07, or fold it into 07's start. Slice 06 is scoped to snapshot outputs only to avoid bundling that work.
- 2026-06-19: ⚠️ **MAJOR: integration branch is built on a stale pre-refactor base.** `feature-references` branched from `main` at `29d0d16` (2026-06-03). Since then `main` advanced **179 commits**, dominated by the **core-arch-simplification refactor**, which **decomposed the ~7,000-line monolithic `src/store/projectStore.ts` into a 363-line shell + ~23 new slices/helpers** (`helpers/referenceTransforms.ts`, `helpers/profileEdit.ts`, `helpers/transform.ts`, `helpers/copyFeatures.ts`, `slices/featureGeometrySlice.ts`, `slices/featureSlice.ts`, etc.). Every function the FR slices edited has MOVED out of `projectStore.ts` into these new files on main. Consequence: a `git merge main` is NOT viable (massive `projectStore.ts` conflicts; auto-merged hunks would duplicate FR-edited functions across the dead monolith and main's new slice files → duplicate symbols / broken build). **This is a PORT, not a merge.** Additive FR files port cleanly (`resolveFeatures.ts`, `featureDefinitions.ts`, new tests, `types/project.ts` additions auto-merged with no conflict); `instanceTransforms.ts` (slice 04) must be reconciled with main's `transform.ts`/`referenceTransforms.ts` to avoid duplicate matrix helpers. **Decision (user):** let in-flight slice 06 finish and be reviewed/merged on the current stale base, then PORT slices 01–06 together onto current `main` (fresh integration branch), then resume 07–08 on the new base. No further slices to be dispatched against the stale base after 06. A dedicated port plan/handoff will be authored before the port begins.
- 2026-06-19: Slice 06 report reviewed at commit `d12e36d` (based on integration `5388ee4`). Diff in scope (new `createSnapshotDefinition`/`gcOrphanedDefinitions` in `featureDefinitions.ts`, `DerivedFeatureFactory` rethreaded to return `{feature, definition}` in `derivedFeatures.ts`, `mergeSelectedFeatures`/`cutSelectedFeatures`/`offsetSelectedFeatures`/`applyProfileBreak` in `projectStore.ts`, `pendingCompletionSlice.ts` dep-type updates, 1-line `SketchCanvas.tsx` unpack, new `snapshotOps.test.ts`). Verified: inputs resolved via `resolveFeatureInstances`; result = definition + identity instance with explicit `definitionId`; merge-defs-then-GC ordering keeps new results and drops orphaned consumed defs; sibling isolation + transformed-input + undo (instances and orphaned defs) covered by 13 tests; build green (30 test files). Found a robustness gap (recorded as port note in row 06): `syncIdCounter` ignores `featureDefinitions`. Accepted and merged into integration. This is the final slice merged on the stale base; next action is the PORT of 01–06 onto current `main`.
- 2026-06-19: **PORT COMPLETE.** Authored `planning/FEATURE_REFERENCES_PORT_Plan.md` (FR-change→main-home mapping, reconciliations, verification) and created fresh integration branch `feature-references-v2` off current `main` (`9615e63`). Port delegated to Codex (per repo convention for large edits); executed at commit `e92cd75`. Behavior distributed to main's new homes: migration→`projectStore.ts` `normalizeProject`; transforms→`helpers/referenceTransforms.ts`/`helpers/copyFeatures.ts`/`slices/featureSlice.ts`(align/distribute)/`slices/pendingCompletionSlice.ts`(move); definition editing→`helpers/profileEdit`-adjacent `slices/featureGeometrySlice.ts`+`slices/selectionSlice.ts`+`helpers/featureDefinitions.ts`; snapshot ops→`slices/featureSlice.ts`+`helpers/derivedFeatures.ts`. New helpers `resolveFeatures.ts`/`featureDefinitions.ts`/`instanceTransforms.ts` + all 6 FR test files added; slice-03 read-paths extended to main's new canvas hooks (`useClickPlacement`/`usePointerGestures`/`useCanvasContextMenu`). **Management review (verified independently, not just Codex's report):** all 8 FR `npx tsx` suites PASS; `npm run build` GREEN (tsc -b + full `npm test` 54 files + vite). Reconciliations confirmed: `instanceTransforms.ts` is matrix-builders-only (no duplicate transform helpers); `syncIdCounter` AND `nextUniqueGeneratedId` now scan `featureDefinitions` keys (def-id collision gap CLOSED); read-path raw-fallback contract preserved (explicit `definitionId`→no fallback, transitional→raw). Scope clean; `planning/` untouched by Codex. **Port ACCEPTED. `feature-references-v2` is now the live integration branch.** Old `feature-references` superseded. Next: resolve the creation-definition gap (small slice) before slice 07.
- 2026-06-19: Slice 06.5 Creation definitions handoff prepared from integration commit `85530d4` (on `feature-references-v2`) and marked Assigned. User chose a dedicated slice (over folding into 07). Scope: make all creation paths mint a `FeatureDefinition` + identity instance — primarily at the central `addFeature` chokepoint (`featureSlice.ts`, after normalization), plus the import bulk paths (`importMergeSlice.ts`) and `.camj` definition merge (collision-safe). Idempotent (skip features that already have a `definitionId`, e.g. snapshot results). Generalize the slice-06 `createSnapshotDefinition` into a shared minting helper. Required before slice 07 (Duplicate as Reference needs every feature to have a definition).
- 2026-06-19: Slice 06.5 report reviewed at commit `d856d9b` (based on integration `4841867`). Diff in scope (new shared `createDefinitionForFeature`/`cloneDimensions` in `featureDefinitions.ts`; `addFeature` mints def+identity transform post-normalization for features without explicit `definitionId`; `importShapes` mints per imported feature; `mergeCamjFolders` remaps source `featureDefinitions` collision-safe via the stateful `nextId('f-')` and mints for definition-less imports, adds defs to `collectExistingIds`; new `creationDefinitions.test.ts` 10 tests + `INDEX.md`). Verified independently: idempotency (explicit `definitionId` left untouched in all 3 paths); the import `.map`'s repeated `createDefinitionForFeature(s.project, f)` does NOT collide because `genId` advances a module-level `idCounter` per call (test asserts 2 features → 2 distinct definitions); `.camj` uses its own stateful generator; minted `f-` ids match migration's feature-id-keyed convention and are unique against features/defs (both scanned by the id system). All FR `npx tsx` suites pass; `npm run build` green on the slice and again on the merged integration state. Accepted and merged into `feature-references-v2`. **Creation-definition gap CLOSED — slice 07 unblocked.**
- 2026-06-19: Slice 07 report reviewed at commit `7d26bf6` (based on integration `3111192`). Diff in scope (`copyMode` on `ProjectMeta` + normalize + `setCopyMode`; `buildCopiedFeatures` reference/independent split + `extractClonedDefinitions` in `copyFeatures.ts`; `startCopyFeature(copyMode?)` + `PendingMoveTool.copyMode` + `completePendingMove` wiring; 4 context-menu items in `FeatureContextMenu.tsx` + `useFeatureTreeActions.ts` + `useTreeContextMenu.ts`; linked badge in `FeatureTree.tsx`; Shape-vs-Instance `PropertiesPanel.tsx`; `duplicateReference.test.ts`). Verified independently: reference copy keeps `definitionId` via spread and bakes profile from `resolveProfile(definition, newTransform)` (correct signature, no double-translate); independent copy clones the definition per source and merges via `extractClonedDefinitions`; `copyMode` defaults reference + normalized for legacy + undoable setter; select-linked uses `getInstanceIdsForDefinition`. All 7 FR suites + `npm run build` green. **Management review found one defect:** the linked badge rendered a blank box — `<Icon id="link">` resolves to `public/icons.svg#link` which did not exist, and `.tree-linked-badge`/`.tree-icon--link` had no CSS (invisible to tsc/tests, so build stayed green). Per ARCHITECTURE.md §5, `icons.svg` is GENERATED from `src/assets/icons.camj` via `npm run sync-icons` — must not be hand-edited. Per user decision, management closed the gap (fix-up `b3ebfaa`): added a `link` icon (two overlapping rings, 4-bezier each) as a feature folder in `icons.camj`, regenerated `icons.svg` (converter: 94 icons/586 features; every other icon byte-identical so no geometry altered), and added badge CSS in `layout.css`. Build green on the fix-up and on the merged integration. Accepted and merged into `feature-references-v2`. Note: `icons.camj` shows a large diff (JSON.stringify whitespace reformat) but the generated sprite delta is exactly the 4-line `link` symbol, proving no existing icon changed. Remaining: slice 08 integration + browser/tablet validation (incl. badge visual, duplicate/make-unique flows) then final PR to `main`.
- 2026-06-20: User live-testing findings on `feature-references-v2`. (1) **Feature-tree row layout bug (FIXED, `e97130b`):** `.tree-row` is a 3-column grid (branch | label | actions); the linked badge was an extra direct grid child, overflowing into an implicit 2nd row and pushing the row action buttons below the name (the region `mask` badge had the same latent bug). Fixed by wrapping label + inline badges in a `.tree-label-wrap` container occupying the middle column; verified in the running app (badged + unbadged rows now single-line/consistent). (2) **Edit-Sketch inconsistency (OPEN, design decision needed):** editing a transformed copy yanks it to the definition-local/original location (it visually "disappears" from its own spot, drawn over the original) while editing the identity-transform first instance edits in place — because `enterSketchEdit` rebakes the edited instance with `IDENTITY_MATRIX` (`featureDefinitions.ts` `rebakeAllInstances` `editingFeatureId` → identity; `selectionSlice.ts:574`). This is the slice-05 "edit the canonical untransformed definition" model and the Plan's open question #5 ("may feel visually surprising"). User finds it inconsistent/confusing. Candidate fix: edit-in-place (keep the clicked instance's transform during edit, inverse-map edits back to definition-local on apply) so all instances behave like the first; a focused follow-up slice. Awaiting user direction.
- 2026-06-20: User chose **edit-in-place**. Authored slice 09 handoff `planning/FEATURE_REFERENCES_Handoff_09_Edit_In_Place.md` (off `origin/feature-references-v2`) and marked Assigned. Scoped to the central choke point: `enterSketchEdit` stops forcing identity; `syncEditedFeatureDefinition` (`featureGeometrySlice.ts`) inverse-transforms the edited world-space profile back to definition-local before writing to the definition; add `invertMatrix` to `instanceTransforms.ts`; remove the now-dead `editingFeatureId` rebake path. Non-uniform/mirror radius/length mapping left as the documented v1 limitation. Must land before the final PR to `main`.
- 2026-06-20: User testing observation re: constrained child + linked copies. **CORRECTION to first take below:** an earlier note here claimed this was a pre-existing, non-FR limitation — that was WRONG (I mis-read the re-solve path). User verified empirically: editing the **host directly** DOES re-solve its constrained child correctly; the bug is that editing a **linked 2nd instance** propagates a shape change to the 1st instance (shared definition rebake) but does NOT re-solve features constrained to the 1st instance. **This is an FR-INTRODUCED bug** (no linked instances exist on `main`, so the scenario can't occur there). Mechanism: in the geometry-edit actions the constraint pass runs against the feature map BEFORE `syncEditedFeatureDefinition` rebakes the *sibling* instance(s); after the rebake changes a sibling's geometry, nothing re-runs the dependent-constraint solve for features bound to that sibling (`featureDefinitions.ts` rebake has no constraint handling). Fix direction: after the definition rebake, re-run the same dependent-constraint re-solve that a direct edit performs, for every rebaked instance id. Exact solver entry to be traced by the implementer. Decision pending (fix as an FR slice before PR vs fold into constraints session). Relates to [[project_sketch_constraints_external]].
- 2026-06-20: User chose **fix now as an FR slice**. Authored slice 10 handoff `planning/FEATURE_REFERENCES_Handoff_10_Linked_Constraint_Resolve.md` (base `915fb0c`), marked Assigned. Scope: after `syncEditedFeatureDefinition` rebakes sibling instances, re-run the dependent-constraint re-solve for all rebaked instance ids, mirroring the direct-edit path. Handoff makes "trace exactly what repositions a dependent on a direct edit" the explicit first step (management traced the path but could not pin the precise repositioning call — `moveFeatureControl` propagates `{0,0}` and `validateConstraintsOnFeature` only refreshes cache/flags, yet direct edits demonstrably re-solve; the move/resize path in `pendingCompletionSlice` repositions via `validateAllConstraints(propagateConstraintsOnTranslate(...))` / `solveFeatureTranslation`). Out of scope: copying the constrained child (separate constraints session).
- 2026-06-22: **Bug (user testing): copying a feature in an OLDER/migrated project doesn't link** — no badge, no Make-Unique/Select-Linked context items on the copy or source; but copying a feature CREATED in-session does link. **Root cause (confirmed in code):** migration builds `featureDefinitions[feature.id]` for legacy features (`projectStore.ts:191`) but leaves their explicit `definitionId` UNSET (they resolve via the `getDefinitionId`→`feature.id` fallback). The reference-copy branch in `buildCopiedFeatures` (`copyFeatures.ts:143–158`) computes `definitionId = getDefinitionId(sourceFeature)` (line 137) and bakes from it, but **never writes `definitionId` onto the copy** (the independent branch does, line 190). So a created source (explicit minted `definitionId`, slice 06.5) links by virtue of the `...sourceFeature` spread, while a legacy source (no explicit id) yields a copy that falls back to its OWN new id → not linked → no badge/context (both derive from `getInstanceIdsForDefinition`). **Fix:** add `definitionId,` (= `getDefinitionId(sourceFeature)`) to the reference-copy `created.push({…})` object; `featureDefinitions[source.id]` already exists from migration so the copy resolves correctly and both rows share the definition. One-line fix; fixes badge + context together; no-op for the already-working created-feature case. To dispatch as a small fix slice with a regression test (legacy/migrated source → copy → assert shared `definitionId` + `getInstanceIdsForDefinition` length 2 + resolves at offset). FR-introduced; fix before PR #156 merges.
- 2026-06-22: **PR #156 review fixes ACCEPTED + merged into `feature-references-v2`** (slice `fr-pr156-review-fixes`, `a0a49cb`). **P1a:** `camj.ts:402` → `feature.transform = feature.transform ?? IDENTITY_MATRIX` (preserve imported instance placement on remap; mint branch unchanged) + `camj.test.ts` case (offset instance keeps `transform.e=50`, resolves at x≈50). **P1b:** `updateFeature` now, when operation changes on a feature with a `definitionId`, updates `definition.operation` and propagates operation + `folderIdForOperation` + `normalizeFeatureZRange` to ALL siblings (`getInstanceIdsForDefinition`) with a per-sibling `isFirst` guard, single undo; new `updateFeatureOperationPropagation.test.ts` (3) — def+both rows+resolver agree, undo restores, unique-path unchanged. Operation confirmed definition-owned/shared. **P2:** e2e helper transform array→object `Matrix2D`; smoke strengthened with non-vacuous `Number.isFinite` + placement assertions on copied/made-unique geometry. **Management review (independent):** all 3 diffs reviewed; camj + propagation tests pass; `npm run build` GREEN (64 test files); **`npm run test:e2e` re-run by management — 10/10** (required a targeted `@playwright/test@1.61.0 --no-save --no-package-lock` install into the shared `node_modules`, which the post-Phase-4 cleanup had dropped since `main`'s package.json lacks Playwright; install left the worktree git-clean). No further bugs. Merged; **PR #156 auto-updates** from the v2 push. **Env note:** running `npm run test:e2e` locally needs `@playwright/test` in `node_modules` — present only after the PR lands on `main` + an `npm install`, or via a targeted install meanwhile.
- 2026-06-22: **PR #156 review — 3 findings triaged + remediation handoff authored** (`planning/FEATURE_REFERENCES_Handoff_PR156_Review_Fixes.md`, branch `fr-pr156-review-fixes`). All three independently confirmed in code: **P1a** (`camj.ts:402`) the definitionId-remap branch forces `transform = IDENTITY_MATRIX`, discarding imported linked instances' placement → resolver renders from def origin while baked sketch shows old location (mint branch :430 is correct, leave it); fix = preserve `feature.transform` on remap. **P1b** `operation` is definition-owned (`resolveFeatures.ts:436` sets `operation: definition.operation`) but `updateFeature` (`featureSlice.ts:449`) patches only the instance row → resolver + siblings diverge; fix = route operation patches through `definition.operation` + propagate/rebake all instances, preserving the row's folder/z-range/stock side effects. **Decision: operation stays definition-owned (shared across linked copies)** — consistent with the resolver; flagged to user that changing one linked copy's operation changes all (per-instance operation would be a larger model change, not taken). **P2** e2e `featureReferences.helpers.ts:86` serializes transform as array `[1,0,0,1,tx,ty]` but `Matrix2D` is `{a,b,c,d,e,f}` → NaN coords, masked because the smoke only checks badges; fix = object matrix + assert finite/placed geometry. Three disjoint files → one remediation slice. Each fix gets a regression test. Ready for user to dispatch; management merges on return.
- 2026-06-22: **PR to `main` OPENED — [#156](https://github.com/PureCutCNC/purecutcnc/pull/156)** (`feature-references-v2` → `main`, 51 commits / 87 files, +15874/−370). User gave explicit go to open (testing sign-off). Build re-confirmed green at tip `f2a926c` (63 test files) + `test:e2e` 10/10 before opening; `main` is a clean ancestor (no rebase). Body covers slices 01–10 + 06.5, UI, the corrected compatibility behavior, version warning, circle/arc fixes, the regression suite + dev-only prod-inert `__pcTest` seam, deferred constraints work, and the two backlogged non-FR CAM gaps. **Plans NOT yet archived** — leave `In progress` until the PR is merged (per no-premature-archive rule); archive + remove INDEX entries as part of the merge.
- 2026-06-22: **Regression Handoff 2 (Phase 3 audit-and-fill) ACCEPTED + merged into `feature-references-v2`.** Slice `regression-tests-2-audit-fill` (`081885b`). Tests-only (verified: 0 non-test/non-INDEX files touched). Adds 3 suites — `camOperationSmoke.test.ts` (10: all 11 OperationKinds smoked generate+post; new pocket `parallel`/`waterline`; 4 drill types), `editOpFidelity.test.ts` (4: insert/delete point, disconnect, arc-handle — each with segment-kind preservation + linked-instance propagation), `featureLifecycleOps.test.ts` (17: stock create/change/undo, `setStockSourceFeature`(+null), delete-source→stock-reset, tab auto-place/update/delete/edit, align/`distributeFeatures` HG/HC/VG/VC + undo). **Management review (independent):** all 3 suites pass (10/4/17); `npm run build` GREEN (63 test files); base = clean descendant of v2 tip. Audit log confirmed it filled real holes and skipped already-covered ground (the "COVERED-skipped" decisions check out — e.g. disconnect geometry was in `openProfileJoin.test.ts` but NOT linked propagation, correctly filled). **Two pre-existing gaps SURFACED (triaged: NOT regressions, NOT FR-introduced, out of FR-PR scope — documented in-test as NOTE comments, raised to user for backlog):** (1) **stock-target operations** — `Operation` accepts `{source:'stock'}` but no toolpath resolver implements it (`resolvePocketRegions` etc. require `source==='features'`); long-standing model-vs-impl gap. (2) **canned drilling cycles** — `MachineDefinition.cannedCycles` schema exists but `runPostProcessor` never emits G81/82/83/73; all drill moves post as G0/G1 (drill-type differentiation lives at toolpath-gen level, so drilling works but without canned-cycle G-code). Both predate the FR work (no FR code touches CAM resolvers or the post). **Regression Phases 1–4 now all merged.** Next regression: none planned — Phase 3 was the last fill.
- 2026-06-22: **Phase 4 browser smoke (Playwright) ACCEPTED + merged into `feature-references-v2`.** Slice `regression-tests-phase4-browser-smoke` (`8323c66` initial + `8bcdb3a` scaffolding restructure). STEP 0 passed — app boots in plain Chromium with zero console errors, NO Tauri stubs needed (`src/platform/index.ts` falls back to browser platform when `window.__TAURI_INTERNALS__` is absent). Ships: `playwright.config.ts` (chromium, dev server :1420), `e2e/` reusable scaffolding (`fixtures.ts` = booted `app` page + auto console-error guard + `ui`; `selectors.ts` single source of truth; generic `helpers.ts`; FR-specific `featureReferences.helpers.ts`; `README.md` with "Adding a test" recipe + worked CAM example), `featureReferences.smoke.spec.ts` (10 assertions: boot/badge-visibility/row-layout/make-unique/select-linked/copy/edit-sketch/properties/save-load/version-warning), a `test:e2e` script (NOT in `npm test` — gate stays browser-free), and ONE guarded product touch: `window.__pcTest` dev seam in `src/main.tsx` (`getProject`/`loadProject`/`getPendingMove`/`completePendingMove`, all inside `if (import.meta.env.DEV)`). **Management review (independent, not just the report):** `npm run build` GREEN; **prod-inert verified by my own grep — 0 `__pcTest` in `dist/`**; **`npm run test:e2e` re-run by management — 10/10 (26s), Playwright spawned + tore down its own dev server (port was free), nothing left running**; scaffolding confirmed extensible (a new feature-area smoke = one spec file on fixture+helpers, no harness-internal changes). Extensibility requirement (added per user) met. No bugs surfaced. **Accepted documented limitations (non-blocking):** (a) the Copy test completes *placement* via the `__pcTest` seam because headless canvas pointer events don't reliably set from/to points — but the context-menu→`startCopyFeature` wiring IS verified through a real DOM click + `getPendingMove()`; (b) runs against the `vite dev` build (not `preview`) since the seam is `import.meta.env.DEV`-guarded — minor dev-vs-prod fidelity trade-off, documented in `e2e/README.md`; (c) no CI integration (explicit non-goal). Phase 4 done.
- 2026-06-22: **Regression Handoff 2 (Phase 3, audit-and-fill) authored** — `planning/REGRESSION_TESTS_Handoff_2.md`, branch `regression-tests-2-audit-fill` off v2. Management did the coverage audit up front so the handoff points at real holes (not re-coverage): **Area A** per-operation CAM smoke across the 11 `OperationKind`s (`project.ts:424`) — confirmed gaps = pocket `parallel`/`waterline` patterns (only `offset` tested), drill cycle types `simple`/`peck`/`dwell`/`chip_breaking` (not differentiated; should assert the posted canned cycle), and thin `v_carve`/`surface_clean`/`follow_line` (single refs — verify generate+post); dedicated suites already cover rough/finish/finish_cleanup/vcarve_recursive/edge_route. **Area B** sketch-edit op fidelity (segment-kind preservation + linked-instance propagation) for the ops H1/editInPlace didn't cover (insert/delete point, disconnect, arc-handle, profile-break, open-join). **Area C** stock/tabs/align-distribute lifecycle (zero existing test hits — real actions confirmed in `types.ts`: `setStock`/`setStockSourceFeature`, tab actions, `alignFeatures`/`distributeFeatures` on ≥3). Tests-only; STOP+report on real bugs (same triage pattern as delete→GC). Independent of Phase 4 — can run in parallel. Ready for user to dispatch.
- 2026-06-21: **Regression Handoff 1 (Phases 1–2) ACCEPTED + merged into `feature-references-v2`.** Slice `regression-tests-1-geometry-lifecycle` (result `c8a0ede`, fix `24a8832`). Adds `src/store/geometryFidelity.test.ts` (116 tests: 8 FeatureKinds × 6 transform classes — `resolveProfile` segment-kind preservation + reference-point mapping + mirror-flip; per-kind edit round-trip; duplicate-as-reference; per-kind store transforms) and `src/store/featureLifecycle.test.ts` (25 tests: create→definition+identity-instance; save/load round-trip incl. mixed linked/unique/independent with `copyMode`+links; undo/redo create/edit/transform/delete; delete→GC). **Bug surfaced + fixed in-slice (triaged, user chose fix-now):** `deleteFeatures` never ran `gcOrphanedDefinitions`, so deleting the last instance of a definition orphaned it in `featureDefinitions` and persisted it into saved files — the lone path missing the GC every sibling op (merge/cut/snapshot, pending-completion) already does. Fix = one line in `deleteFeatures` (`gcOrphanedDefinitions(survivingFeatures, defs).definitions` in the nextProject spread, mirroring `featureSlice.ts:669`; undo untouched — history clones the full prior project). Test tightened from the merge-path GC workaround to a **direct** delete→GC assertion (last-instance GC + undo restores both; linked-pair partial-keeps/last-GCs). **Management review (independent, not just the report):** both suites 116/0 + 25/0; product diff verified in scope (deleteFeatures only); `npm run build` GREEN (tsc -b + full `npm test` 60 files + vite) before and after merge. The original circle/arc bug class is now systematically covered. Known residual (documented, not a regression): nothing — the delete→GC gap is closed. Next regression work: Handoff 2 (audit-and-fill, Phase 3) after this; Phase 4 Playwright smoke handoff already authored.
- 2026-06-21: **Decision (user): Phase 4 browser smoke = build a thin Playwright harness** (over keep-it-manual / decide-after-H1). Rationale recorded for the user: of the last four escaped bugs, two were store-layer (huge-circle radius, arc flattening — owned by the geometry suites) and two were DOM/wiring-layer (blank linked badge: missing icon+CSS; tree-row overflow) that tsc + every test passed and only a browser caught. Playwright's value is exactly that wiring/DOM layer; it is explicitly NOT for geometry (store suites own it) or WebGL pixels (opaque + GPU-flaky). Scope = a repeatable ~30–60s `npm run test:e2e` smoke the user runs before each manual session. Authored `planning/REGRESSION_TESTS_Handoff_Phase4_Browser_Smoke.md` (branch `regression-tests-phase4-browser-smoke` off v2) and updated `REGRESSION_TESTS_Plan.md` Phase 4. Greenfield (no Playwright in package.json). Handoff gates on STEP 0 feasibility (must boot in plain Chromium without heavy Tauri mocking, else fall back to a manual checklist) and permits ONE product-code touch: a minimal dev/test-guarded `window.__pcTest` seam (seed/read project via the real `openProjectFromText`) so the smoke avoids flaky canvas-drawing while still asserting through the real DOM + menu→action wiring. Independent of H1/H2 — can run in parallel or after H1.
- 2026-06-22: **Phase 4 handoff strengthened — extensibility now a REQUIRED deliverable** (user: coverage should be easy to extend as features are added). The harness must ship as reusable scaffolding — a Playwright fixture (auto console-error guard + `__pcTest` seam + injected helpers), a single `e2e/selectors.ts` (one place for UI churn), generic `e2e/helpers.ts` primitives (`seedProject`/`getProject`/`treeRows`/`openRowContextMenu`/…), and an `e2e/README.md` "Adding a test" recipe + worked example — with the FR smoke built on top to prove the pattern. Added acceptance criterion: a new feature-area smoke = one spec file using fixture+helpers, no harness-internal changes. If Phase 4 was already dispatched against the prior handoff, relay this addition or enforce it at review.
- 2026-06-21: Started a **regression-test build-out** (user request), folded into `feature-references-v2` so it ships with this PR. `planning/REGRESSION_TESTS_Plan.md` defines the matrix (store-level `npx tsx` suites, primary; browser smoke deferred to a separate harness). Authored `planning/REGRESSION_TESTS_Handoff_1.md` (Phases 1–2: geometry-fidelity matrix shape×transform×round-trip + feature lifecycle save/load/undo/delete-GC) — branch `regression-tests-1-geometry-lifecycle` off v2. Handoff 2 (audit-and-fill editing/boolean/CAM) authored after H1 lands. Motivated by the circle + arc bugs that no test caught.
- 2026-06-21: **Bug fix (user testing): composite arcs were flattened to splines.** `resolveProfile` converted `arc` segments to beziers *unconditionally* (even at identity), unlike circles which it preserves under `isCirclePreservingTransform`; `transformProfileAffine` (inverse-bake / rigid moves) also converted arcs to beziers. So every rebake (edit / duplicate-as-reference / move) of an arc composite lost the arcs. Fix: `resolveProfile` now keeps arcs as arcs under arc-preserving (similarity) transforms — transform `center`+`to`, flip `clockwise` on mirror — and only converts to beziers under shear/non-uniform; `transformProfileAffine` keeps arcs (transform `center`+`to`) so the canonical definition isn't flattened. Verified in-app end-to-end (arc composite: `[arc,line,line]` preserved through create/edit/duplicate-as-reference/move) + 2 new regression tests in `editInPlace.test.ts` (14/14). `npm run build` green. Same family as the circle-center fix.
- 2026-06-21: **Compatibility assumption CORRECTED (user cross-version test).** Earlier docs claimed an old (pre-references) build *flattens* links when it re-saves a `2.0` file — that is WRONG. Verified: an old build **preserves** `featureDefinitions` + `definitionId`/`transform` across load+save (it spreads through unrecognized fields), so links survive an old-build round-trip and v2 still shows them. v2 confirms this in code: `normalizeProject` does not rebake on load (`features` kept as-loaded, line 174) and keeps loaded `featureDefinitions` when present (line 202); the 2D canvas renders from `feature.sketch.profile` (no resolver in `SketchCanvas` render), so a divergent baked sketch shows as-is. **Real caveat:** editing a linked instance in an old build updates only that instance's baked `sketch`, not the shared definition → it diverges; v2 shows the divergence until the next definition edit re-bakes all instances from the (unchanged) definition, silently discarding the old-build edit (never propagates to siblings). Corrected `ARCHITECTURE.md` §4 (resolver-boundary + dual-storage paragraphs). **Decision (user): document only** — no active divergence handling on load (niche, transitional: only when a 2.0 file is edited in a pre-references build). The corrected ARCHITECTURE §4 caveat is the record.
- 2026-06-21: **Bug fix (regression found in user testing): circle radius edit produced a huge circle for moved/copied circles.** Root cause: `transformProfileAffine` (`helpers/transform.ts`) didn't transform a circle segment's `center` (only `to`), so slice-09's inverse world→definition bake corrupted the radius for any non-identity-transform circle (identity circles were unaffected — matches "works on a fresh circle"). Asymmetric with `resolveProfile`, which does transform circle center. Fix: added a `circle` branch to `transformProfileAffine` transforming both `center` and `to` (also fixes a latent bug in the other caller, `referenceTransforms.ts`, for rigidly-moved circle dependents). Verified in the running app (moved circle: radius edit 10→15, was 85; center stays put) and with a new regression test in `editInPlace.test.ts` (12/12). `npm run build` green.
- 2026-06-21: Documented the implemented model in `ARCHITECTURE.md` — new §4 "Feature References (Definitions & Instances)" covering the definition/instance split, resolver boundary, **dual-storage backward compatibility** (baked `sketch` for old builds; old-build re-save flattens links), and versioning/forward-warning; expanded §3 data-model bullets; fixed the pre-existing duplicate `## 6` numbering (now 1–9). Added a top-of-file **Status** block to `FEATURE_REFERENCES_Plan.md` (points to the ledger as live status and ARCHITECTURE §4; lists delivered slices 06.5/09/10 + refinements + copyMode/constraints decisions) and corrected the stale branch-model section to `feature-references-v2`. ARCHITECTURE §4 ↔ Plan ↔ Ledger now cross-link.
- 2026-06-20: **Decision (user): keep `copyMode` as-is** — "Copy" makes a linked (reference) copy by default; "Make Unique" breaks the link; no `copyMode` UI toggle. Orphaned `duplicateAsReference`/`duplicateIndependent` handlers in `useFeatureTreeActions.ts` left in place (harmless) unless cleanup is requested.
- 2026-06-20: User request — add a load-time warning when a `.camj` file's `version` is NEWER than this app supports (forward-compat for FUTURE model changes; does not affect the current 1.0/2.0 dual handling). Implementing on v2 (tied to the FR version bump): `LATEST_PROJECT_VERSION` constant + version-compare helper; `openProjectFromText` (the universal text-load entry — file-open, examples, desktop OS-open all route through it) sets a transient `loadWarning`; App surfaces it once via `window.alert` (matches existing import-warning pattern) and still loads best-effort.
- 2026-06-20: Non-FR UI tweak folded into the branch (user request, `dcd37bf`): hide the row three-dot (⋮ "more actions") button on **desktop** — redundant with right-click (tree row + canvas) — by gating `onMoreMenu` on `tabletShell` in `FeatureTree.tsx`; kept for tablet (no right-click). Applies to feature/tab/clamp rows. Note in the final PR body as an included unrelated improvement.
- 2026-06-20: Context-menu refinement (user feedback during slice-08 testing). Removed **Duplicate as Reference** and **Duplicate Independent** from the feature context menu ("we don't need these two" — `Copy` already duplicates per project `copyMode`, default reference). Moved **Make Unique** + **Select Linked Instances** from the middle of the menu to the **top** (they were "placed in the middle of the existing choices"), and made that group render only when the feature is actually linked (`menuFeatureHasLinkedInstances`) instead of always-shown-but-disabled. Build green. NOTE: the `duplicateAsReference`/`duplicateIndependent` handlers in `useFeatureTreeActions.ts` are now orphaned (harmless); `copyMode` + `Copy` unchanged — open question whether to keep/expose `copyMode` or simplify `Copy` to a single behavior (raised to user).
- 2026-06-20: User reported a second symptom — after **Make Unique** on a moved/resized instance, Edit Sketch still opens at the original position/size. Confirmed **same root cause**: `makeUnique` (`featureDefinitions.ts:305`) clones the canonical/untransformed definition and keeps the instance transform, so the identity-force shows the clone at the origin. Folded into slice 09 — added an explicit acceptance criterion + test (make-unique-then-edit-in-place with translate+scale). No separate slice needed.
- 2026-06-19: Slice 08 UX adjustment — linked-instance badge. User feedback: the badge as styled "ruins the feature tree UI" and its meaning was unclear. Root cause: the slice-07 markup was correct but unstyled, and the management icon fix-up (`b3ebfaa`) gave it a bordered box + background fill, so it read as another row *button* competing with the eye/menu actions. Per user choice (subtle inline glyph), reworked `.tree-linked-badge`/`.tree-icon--link` CSS to drop the box/border/fill — now a quiet 12px muted-blue inline glyph. Verified in the running app: linked rows show only a faint marker, independent rows none; build green. Meaning (for docs/onboarding): the badge marks a feature whose *shape definition* is shared with ≥1 other feature, so Edit Sketch propagates to all linked copies; Make Unique breaks the link. User still to do extensive testing.
- 2026-06-19: **Slice 08 browser validation PASS.** Management drove the running app (`npm run dev` on :1420 from the v2 worktree, chrome-devtools) and exercised every FR flow through the REAL store actions + real read path + real DOM, with zero console errors throughout. Results: (1) creation mints a definition + identity transform (06.5) and `copyMode` defaults `reference`; (2) Duplicate as Reference via the real pending-move path (`startCopyFeature(id,'reference')`→`setPendingMoveFrom`→`completePendingMove`) creates a 2nd instance sharing the source `definitionId`, and the resolver renders it at the correct +offset (no double-bake); (3) Duplicate Independent clones to a distinct definition; (4) linked badge renders on exactly the shared instances (11×11, `/icons.svg#link` resolves, tooltip correct) and the new `link` glyph (two overlapping rings) was visually confirmed; (5) Make Unique repoints the instance to a fresh definition and the badges reactively clear; (6) Select Linked selects exactly the siblings (`getInstanceIdsForDefinition`); (7) all instances resolve through the read path; (8) **transformed-instance edit through the full sketch-edit pipeline** (`enterSketchEdit`→`filletFeaturePoint`→`applySketchEdit` on the −25-offset instance) propagated the fillet to BOTH linked instances while preserving the instance transform exactly (A↔B offset stayed −25; both shifted +2 by the fillet geometry) — closes slice-05 risks (c)/(d); (9) Properties panel shows SHAPE vs INSTANCE groups; (10) save/load round-trip via `openProjectFromText(JSON.stringify(project))` is byte-identical for the FR data, persists `copyMode` (tested `independent`), preserves linked relationships, and re-renders badges after load. Controlled Chrome (mcp chrome-profile) and the dev server were killed after. **Not exercised this session (residual risks for user/tablet pass):** non-zero `sketch.origin`/`orientationAngle` stock-source/imported features through rebake; `enterStockSketchEdit` definition-local swap (known slice-05 gap, not a regression); tablet context-menu/long-press/properties. Minor cosmetic note (non-blocking): independent-clone ids use `f-f0004` and Make Unique uses `def-clone-N` — unique and correct, just inconsistent id namespaces.
- 2026-06-19: Slice 07 UI workflow handoff prepared from integration commit `3111192` (on `feature-references-v2`) and marked Assigned. `planning/FEATURE_REFERENCES_Handoff_07_UI_Workflow.md`. Scope confirmed against Plan §"UI surface": (1) project `copyMode: 'reference'|'independent'` in `ProjectMeta` (default reference, normalized for legacy, setter); (2) explicit `duplicateAsReference` (new instance, same definition) and `duplicateIndependent` (clone definition) store actions, with the existing duplicate path following `copyMode` and fixing the `buildCopiedFeatures` reference-case double-bake; (3) context-menu items Duplicate as Reference / Duplicate Independent / Make Unique (wire existing `makeUnique`) / Select Linked Instances; (4) feature-tree linked badge via `getInstanceIdsForDefinition`; (5) Properties panel shared-vs-instance grouping. Browser/tablet validation reserved for management (slice 08). Note for review: most of the model layer already exists (`makeUnique`, `getInstanceIdsForDefinition`, `getDefinitionId`, `selectFeatures`, clone/mint helpers) — slice 07 is wiring + thin actions, NOT new geometry semantics.
